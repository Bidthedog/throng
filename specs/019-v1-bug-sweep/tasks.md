# Tasks: v1.0.0 Bug Sweep — Signals That Were Never Sent

**Feature**: 019 | **Branch**: `019-v1-bug-sweep` | **Date**: 2026-07-16

**Input**: [spec.md](./spec.md) · [plan.md](./plan.md) · [research.md](./research.md) ·
[data-model.md](./data-model.md) · [quickstart.md](./quickstart.md) · [contracts/](./contracts/)

---

## Test discipline (read before touching a line)

**The RED half of the cycle is already done and committed.** Seven test files exist, carrying **20 RED
assertions — 8 in the node suite (4 theme + 2 open-on-click + 2 pty-timeout) and 12 in E2E (6 #87 + 2
#86 + 4 #67) — and 19 green regression guards** (1 AC7 + 2 Terminate All + 16 theme), all written
against `87e28a9` before any production change
and each confirmed red *for the right reason*. **No task below re-creates them.** This feature is
**Green, then Refactor** — and the guards sitting green beside the RED assertions are as load-bearing as
the RED ones.

| File | Story | Status |
|---|---|---|
| `packages/ui/tests/e2e/editor-move-repoint.e2e.ts` | US1 / #87 | **6 RED** (AC1–AC6) + **AC7 GUARD, green — must stay green** |
| `packages/ui/tests/e2e/terminate-all-drain.e2e.ts` | US2 / #86 | **2 RED** (ordinary close) + 2 green (pin the Terminate All asymmetry) |
| `packages/daemon/tests/integration/pty-agent-launch-timeout.integration.test.ts` | US3 / #94 | **2 RED**, elevation-free, runs in CI |
| `packages/ui/tests/e2e/terminal-de-elevation-hang.e2e.ts` | US3 / #94 | `@admin` — **never executed anywhere** |
| `packages/ui/tests/e2e/preferences-terminal-flavours.e2e.ts` | US4 / #67 | **4 RED** |
| `packages/core/tests/unit/settings-open-on-click-single-owner.test.ts` | US5 / #95 | **2 RED**, fix-agnostic |
| `packages/core/tests/unit/theme-syntax-body-contrast.test.ts` | US6 / #83 | **4 RED** + 16 green (locks the measured 150) |

**The RED tests are the contract and MUST NOT be rewritten to fit an implementation.** There is exactly
**one** sanctioned amendment in this feature (T054, forced by C3) and it is a clarification of the test's
own stated intent, not an accommodation. No other existing assertion may be weakened.

Where a task needs **new** coverage — C14's `terminals.defaultParams` text column, C6's sub-workspace
drain, **C19's preferences-window drain (T015a)**, **`emptyValueFor('records')` (T036a)**, **C17's
duplicate-id-from-file case (T041a)**, **US3 AC4's slow-shell scenario (T034)**, the pure `path-id`
module, `validateFlavourRecord`, the launch reporter, US5's group move —
Principle V is **NON-NEGOTIABLE**: the test task comes **before** its implementation task, is run, and is
observed failing for the right reason first.

**Tests that encode the bug and MUST be updated when the fix lands** (they assert today's defect; the fix
turns them red, and that is correct):

| Test | Line | Asserts | Fixed by |
|---|---|---|---|
| `packages/core/tests/unit/settings-metadata.test.ts` | `:86` | `terminals.disabledBuiltins` control is `'array'` | T039 (US4) |
| `packages/core/tests/unit/settings-metadata.test.ts` | `:62` | `explorer.openMode.allowedValues` | T052 (US5) |
| `packages/core/tests/unit/metadata.test.ts` | `:44` | leaves contain `explorer.openMode` | T052 (US5) |
| `packages/core/tests/unit/app-settings.explorer.test.ts` | `:7`, `:19`, `:25` | `explorer.openMode` exists/parses/coerces | T052 (US5) |

**Run a suite ONCE, unfiltered, capture the complete output** (constitution v3.14.0); parse the capture as
many times as you like; re-run only failures; then one full run as the evidence. A test that flips with no
code change is **flaky, not fixed**.

**The CI caveat you must not skip past** (corrected — see **C18**): **two** of the seven files call
`skipIfElevated()` — `editor-move-repoint.e2e.ts` and `terminate-all-drain.e2e.ts` — and GitHub's
Windows runners run **elevated**, so those two **self-skip in CI**. A green CI bar says **nothing**
about **#87 or #86**; they are verified on a **non-elevated developer run**, and that output goes in the
PR evidence (T062). **#67 is not in that list**: `preferences-terminal-flavours.e2e.ts` has **no
elevation guard at all**, so it executes in CI — its 4 RED are failing CI on this branch **today**, and
T046 turning them green is visible there. (Every artifact used to say "six of the seven"; three of the
seven are Vitest suites that cannot call a Playwright skip helper, so the claim was impossible on its
face. It was repeated across five documents until it read as fact — the exact class this feature
closes.) Narrowing the guard on the two that do self-skip is out of scope (plan, Complexity Tracking).

## Phase order ≠ priority order

Phases below are laid out in **spec priority order** (US1 → US6) for traceability. The **execution**
order the plan mandates is **US6 → US5 → US3 → US1 → US2 → US4** — cheapest RED first, and one hard
dependency: **US1 must land before US2**, because US1's re-point writes the new path into the panel
config, which rides the very `workspace.save` debounce US2 drains. Fixing the drain first would leave
US1's persistence assertion passing for the wrong reason. See [Dependencies](#dependencies).

---

## Phase 1: Setup (shared)

- [X] T001 Bootstrap the worktree per [quickstart.md](./quickstart.md) §Prerequisites: `npm install` (a fresh worktree cannot resolve `@throng/*` until this runs — junctions, no lock change) then `npm run build` (the E2E suite launches `packages/ui/dist/main/main.js`, so an unbuilt tree fails E2E for a reason that has nothing to do with this feature).
- [X] T002 Capture the node-suite baseline **once, unfiltered**: `npm run lint` (zero errors), `npm run typecheck` (`tsc -b` now covers the renderer — `87e28a9`), `npm test 2>&1 | tee 019-baseline.log` — capture to a **`*.log` at the repo root**, which `.gitignore` already covers, **not** `/tmp`: this is a Windows-first, PowerShell-primary repo where `/tmp` resolves inside the Git-Bash root rather than the worktree, so the capture would land somewhere neither T062 nor T063 can find it. Confirm the bar is exactly where the plan says: **1500 pass, 8 fail**, and all 8 are this feature's guards — 4 in `packages/core/tests/unit/theme-syntax-body-contrast.test.ts`, 2 in `packages/core/tests/unit/settings-open-on-click-single-owner.test.ts`, 2 in `packages/daemon/tests/integration/pty-agent-launch-timeout.integration.test.ts`. A ninth failure is **not** this feature's and must be triaged before anything else.
- [ ] T003 Capture the E2E baseline **once**, non-elevated, retries off: `THRONG_E2E_RETRIES=0 npx playwright test editor-move-repoint.e2e.ts terminate-all-drain.e2e.ts preferences-terminal-flavours.e2e.ts 2>&1 | tee 019-e2e-baseline.log` (repo root, gitignored — see T002). Expect **#87 6 RED + AC7 green · #86 2 RED + 2 green · #67 4 RED**. If AC7 (`packages/ui/tests/e2e/editor-move-repoint.e2e.ts:353`) is not green at baseline, stop — the guard that must survive US1 is already broken and the fix cannot be measured against it.

**Checkpoint**: the bar is known and captured. Every claim below is measured against this file, not re-run to rediscover it.

---

## Phase 2: User Story 1 — A file moved inside throng takes its editor with it (#87, P1) 🎯 MVP

**Goal**: an in-app move re-points every editor open on that file, by **path mutation only** — same buffer, same undo history, same clean state — and never invites the save that undoes the move.

**Independent test**: `THRONG_E2E_RETRIES=0 npx playwright test editor-move-repoint.e2e.ts` — 6 RED go green, AC7 stays green.

- [x] T004 [P] [US1] **RED**: new unit test `packages/core/tests/unit/path-id.test.ts` for the pure normaliser named by FR-007 and `data-model.md` §2 — `normaliseForCompare` (`\`→`/`, strip trailing `/`, lowercase), `samePath`, `isUnderPath`. Cover the cases that make raw comparison fail **silently on Windows**: the coordinator holds forward-slashed paths from the tree while `node:path.join` in main produces back-slashed ones, so `samePath('C:/p/note.txt', 'C:\\p\\note.txt')` is **true**; case differs but the file does not; and the prefix boundary is a **segment** boundary — `isUnderPath('C:/a/package-lock.json', 'C:/a/pack')` is **false** (`f === g || f.startsWith(g + '/')`, the predicate `markDeleted` already uses at `editor-coordinator.ts:270-276`). Run it; confirm it fails because the module does not exist.
- [x] T005 [US1] **GREEN**: implement `packages/core/src/fs/path-id.ts` — pure, **no OS calls** (a normalisation rule is not a platform call, so it belongs in core — Principle II) — and export it from the `@throng/core` barrel (`packages/core/src/index.ts`). Case-insensitivity is a **Windows-first decision**: state it in the docblock so the next reader knows it is a choice, not an accident.
- [x] T006 [US1] **Refactor, behaviour-preserving**: point `markDeleted`'s inline normaliser (`packages/ui/src/main/editor-coordinator.ts:269-276`) at `samePath`/`isUnderPath` so there is **one** copy of the rule, not three (DRY, Principle VIII — the third copy is the RED E2E's own `normPath` helper at `editor-move-repoint.e2e.ts:74-75`, which exists *because* raw comparison makes even the correct AC7 look broken). `markDeleted`'s **behaviour** is untouched — FR-009/AC7 depends on it.
- [x] T007 [P] [US1] **RED**: extend `packages/ui/tests/integration/files-service.test.ts` for the move signal per [contracts/move-signal.md](./contracts/move-signal.md) §1 — the half of FR-001 the E2E cannot see. `onMoveStarted` fires with every resolved **absolute** source path **before the first `fs.move`**; a **half-succeeded batch reports only what ACTUALLY moved**, never the requested list (`move` returns on the first disallowed item, `files-service.ts:100-105`, so this is the ordinary case, not an edge case — it is the lesson `delete` already learned with `removed[]`, `:140-165`); `onMoved` fires in a **`finally`** on success, on the early `return { error }` **and** on a throw (a bracket that never closes leaves a doc `movePending` forever and it can never be dirtied again by a genuine external delete); a same-folder drop (`:98-99`) announces **nothing**; `rename` announces exactly one pair `{from: abs, to: join(dirname(abs), name)}` (**FR-006 — a rename is a move, and it has #87's hole identically at `:62-82`**) and the no-op rename (`:72`) announces nothing. Run it; confirm it fails because the callbacks do not exist.
- [x] T008 [US1] **GREEN**: `packages/ui/src/main/files-service.ts` — export `MovePair`, add `setOnMoveStarted`/`setOnMoved` mirroring `setOnDeleted` exactly, and bracket both `move()` (`:84-112`) **and** `rename()` (`:62-82`) per the contract: accumulate a pair **as each `this.fs.move(...)` resolves**, announce in a `finally`. Keep the current `{ok:true} | {error}` return and the early-return-on-error behaviour — continuing past a failure is a behaviour change, not a defect fix.
- [x] T009 [US1] **GREEN**: `packages/ui/src/main/editor-coordinator.ts` — add `beginMove(absPaths)` and `markMoved(moves)` per [contracts/move-signal.md](./contracts/move-signal.md) §3, plus `movedTo?: string` on `EditorSyncMsg` (`:102`). Per matched doc: match via `samePath`/`isUnderPath` (**never raw equality**); rewrite by **prefix replacement** for a folder move (FR-005/AC6); `unregisterPanel` **then** `registerOpen` — the pair `save()` already uses for Save-As (`:503-505`) — so the new path answers `focus` and the old answers `open` (AC4); `doc.absPath = newAbs` then `this.watchDoc(doc)` (the watch is per-**folder**, `:681-685`, so a cross-folder move **must** re-watch); `relaySync(-1, { panelId, movedTo })` — every window, the authority's ordered stream, as `markDeleted` uses at `:290`; clear `movePending` on **every** doc `beginMove` opened, moved or not. **MUST NOT**: re-`load()` the doc or touch `authority` (FR-002 — a reload mints a second original, Principle XI); call `markUnsaved()`, write a recovery snapshot or raise a notice (FR-003 — AC5 asserts a clean move leaves **no snapshot at all**); alter `markDeleted` in any way (FR-009/AC7).
- [x] T010 [US1] **GREEN**: `packages/ui/src/main/editor-coordinator.ts` `onDiskChange` (`:692-706`) — while `doc.movePending`, take the `!res.ok` branch **without** calling `markDeleted`, and return. This is FR-004's ordering, solved by the **bracket**, not by a clock: it **MUST NOT** gain a timer, a grace period or a retry. A "wait 250ms and re-check" is the `terminate-all` accident in miniature — the exact shape FR-011 condemns one story over. The existing stale-watch guard (`doc.absPath !== watchedPath`, `:694`) already covers the post-re-point case.
- [x] T011 [US1] **GREEN**: wire the signal in `packages/ui/src/main/main.ts`, beside the `setOnDeleted` line at `:586` — `filesService.setOnMoveStarted((p) => editorCoordinator.beginMove(p))` and `filesService.setOnMoved((m) => editorCoordinator.markMoved(m))`.
- [x] T012 [US1] **GREEN**: `packages/ui/src/renderer/editor/use-editor.ts` — handle `msg.movedTo` beside the existing sync handling (`:779-790`): update the panel's config `filePath` and **nothing else**. That is what makes the header pill follow (AC1 asserts `panel-file-<pid>` carries `title` = the new **native-spelled** path, rendered via `toDisplayPath`, `panel-placeholder.tsx:481-483`) and what makes the **persisted layout** carry the new path so a restart reopens on the moved file (FR-008). **MUST NOT** set dirty, clear the buffer, re-request a load, or show the missing-file notice. That config write rides the debounced `workspace.save` — **US2's subject, and why US1 lands first**.
- [x] T013 [US1] Run `THRONG_E2E_RETRIES=0 npx playwright test editor-move-repoint.e2e.ts` **non-elevated**. All 6 RED (AC1–AC6) green; **AC7 still green**. A retried pass is a flake, not a pass. If AC7 went red, the fix swept away the behaviour it guards — a file moved by **another program** must stay kept, dirty and recoverable (FR-009), and that path is **correct**.
- [x] T014 [US1] Run the adjacent guards that share this blast radius and confirm they are **still green**: `packages/ui/tests/integration/editor-file-deleted.integration.test.ts`, `editor-one-buffer.integration.test.ts`, `editor-recovery.integration.test.ts`, `editor-external-change.integration.test.ts`, `files-delete-mixed.integration.test.ts`, `files-move-same-folder.integration.test.ts`, plus `packages/ui/tests/e2e/editor-file-deleted.e2e.ts` and `rename-noop.e2e.ts`. The move signal touches the registry, the watch and the recovery path — the three things these pin.

**Checkpoint**: an in-app move (cut+paste, drag, folder, rename) re-points every open editor; an external move still dirties. US1 ships alone — it is the MVP.

---

## Phase 3: User Story 2 — Closing throng keeps the layout you were looking at (#86, P1)

**Goal**: a layout or zoom change the user could see when they closed is there when they reopen, on **every** exit, **by construction rather than by timing**.

**Independent test**: `THRONG_E2E_RETRIES=0 npx playwright test terminate-all-drain.e2e.ts` — 2 RED go green, the 2 Terminate All tests stay green.

- [x] T015 [P] [US2] **RED**: add a **sub-workspace drain** test to `packages/ui/tests/e2e/terminate-all-drain.e2e.ts` — **C6 requires it and no existing test covers it**. Tear a Tab into a sub-workspace, rearrange panels **in the sub-workspace**, then close the **main** window (which closes the group via `WindowManager.closeChildren` — `window-manager.ts:96`, fired from `:32`, over the windows `registerChild` collected at `main.ts:723`); relaunch and assert the sub-workspace's arrangement survived. Drive `BrowserWindow.close()` — the harness caveat the file documents at `:88-90` is absolute: neither `app.close()` nor the harness teardown reaches this handshake, both **destroy** the window and fire no `close` event, which is precisely why no existing spec covers it. Reuse the file's `ordinaryClose`/`storedPanels` helpers. Run it; confirm it fails for the right reason — a drain that reaches only the main window leaves the bug half-fixed for exactly the multi-window users most likely to have a layout worth keeping.
- [x] T015a [US2] **RED**: the **preferences-window drain** (**C19**; scope per **C21**), added to `packages/ui/tests/e2e/terminate-all-drain.e2e.ts` beside T015 — **no test covers it at any level today**, and Principle V is non-negotiable for the widening C19 decides. **Not `[P]` with T015**: both edit this file, and this file's own convention forbids that (see US4 ✗ US5 below). **ONE tab, ONE pending write** — true of the **settings and themes** tabs, which is why this test drives Themes (C20; the JSON tab is **not** in that set, C21): open Preferences on the **Themes** tab, change a colour, and close the **main** window **inside the 150 ms debounce** so the preferences window goes down with it; relaunch and assert the colour survived. **The mechanism, read rather than assumed**: the preferences window is **not** in `WindowManager`'s cascade — `closeChildren` (`window-manager.ts:96`, fired from `:32`) iterates only windows passed to `registerChild`, which is called **only** for sub-workspace windows (`main.ts:723`). Preferences is **Electron-parented** to the main window (`preferences-window.ts:67`, `parent: mainWindow`) and dies by native parent-child destruction. **The design is unaffected** — C23's drain names no windows and reaches it via `getAllWindows()` — but a RED test staged on a cascade it is not in would fail for a mechanism its author had not understood, which is exactly what C20 was censured for. Drive `BrowserWindow.close()` — the file's `:88-90` caveat is absolute (`app.close()` and the harness teardown **destroy** the window and fire no `close` event, which is exactly why the unmount flush does not save it). **Do NOT assert a settings edit and a theme edit pending together**: `preferences-app.tsx:273-288` renders exactly **one** tab, and switching between **those two** unmounts the previous one and flushes it **synchronously** (C21 confirms they are mutually exclusive; the JSON tab is the exception, being re-rendered rather than unmounted) — that state is unreachable, and a RED test asserting it would fail for a mechanism nobody had understood, which is not "failing for the right reason". Run it; confirm it fails: the window is destroyed with one pending `void writeConfig(…)` and nobody asks it for anything.
  **DONE — and the staging premise above was measured FALSE, so the test landed alongside a second one that does the proving.** Driven, the Themes case **passes with the drain disabled**: the ordinary close destroys the window ~250 ms *after* the drain completes (`main.ts` — the beat that lets the overlay paint), and this debounce is **150 ms**, so its timer always fires on its own well inside that. Themes survives by the same accident Terminate All does, and no assertion staged on it can tell the drain from the clock. Every artifact chose this staging by asking *"can two writes be pending at once?"* (C20/C21) — a question about **pendency**, never about whether a close can **beat** a debounce, which is the only question a drain test answers. The tab-exclusivity reasoning was sound and irrelevant.
  **The preferences drain is instead proved by the JSON tab (300 ms — the one config debounce that outlives the close), added beside it**: RED with `settleConfigWrites()` disabled (`Expected "Matrix", Received "throng"`), GREEN with it enabled. That test also carries C23's real content — the preferences window is a separate **renderer process** with its own copy of the write module, so a drain that named only the workspace window would settle nothing in it. The Themes test is **kept and labelled a guard, not a proof**: it would still catch a regression that lost this write.
- [x] T015b [US2] **Close the measurement gap in SC-005 / US2 AC4**: add the **ordinary-close** language-override case to `packages/ui/tests/e2e/terminate-all-drain.e2e.ts`, reusing the file's `ordinaryClose` helper — set an override, close **immediately** with no terminals, relaunch, assert `document.getState` returns it and the strip reads it. AC4 and SC-005 both claim the override survives **either** exit; only **Terminate All** is measured (`:182-250`), while the two ordinary-close tests are layout (`:259-311`) and zoom (`:313-361`). **Expected green on the first run** — this is a guard, not a RED: the mechanism says it cannot fail (`setDocumentOverride` is an awaited IPC with no debounce onto a **synchronous** SQLite write, `document-service.ts:67`, so no close beats it). State that when you run it, as T034's vacuous-pass step does. **If it is RED, stop**: an override *is* being lost on the ordinary close, which would mean #86 is wider than reproduced and the spec's own correction of #86 is wrong. Not `[P]` — same file as T015/T015a.
- [x] T015c [US2] **RED**: the **workspace-window config drain** (**C23**), added to `packages/ui/tests/e2e/terminate-all-drain.e2e.ts`. **C19's widening got T015a "rather than leaving the widening untested"; C23's widening got nothing** — and C23 is the entry that found a preferences-only gate *"would have acked `projects-panel.tsx:208`'s write in flight — the exposure C21 refused to accept as a limit, shipped inside the fix for it"*. Same argument, same file, same precedent. **Create** a project with a chosen root folder — which is the **only** path to `persistLastProjectFolder` (`projects-panel.tsx:205-209` → undebounced, `void`-dropped `writeConfig({kind:'settings'}, …)`, 011 FR-040; `ProjectsPanel` is rendered by `app.tsx:493`, in the **workspace** window) — then `BrowserWindow.close()` immediately, relaunch, and assert `newProject.lastProjectFolder` persisted. Reuse `new-project-folder.e2e.ts`'s existing drive of that path. Run it; confirm it fails for the right reason: the window is destroyed with that write in flight and nobody asks it for anything. **Not a *rename***: rename goes through `updateProject` → `client.update`, an awaited SQLite IPC that never touches `writeConfig` — an earlier draft of quickstart §6 used it and could not have failed. Not `[P]` — same file as T015/T015a/T015b.
  **DONE, but it is a GUARD, NOT A PROOF, and is now labelled as one in the spec file. It CANNOT be made discriminating, and the reason is a correction to C23.** Measured: it **passes with `settleConfigWrites()` disabled**. `persistLastProjectFolder` is **undebounced**, so `void writeConfig(…)` has already handed the payload to main **over IPC** before the `close` event fires; main completes the file write whether or not the renderer is alive to await it. **A dropped promise loses the RESULT, never the write.** There is therefore no deferred state on this journey for the drain to settle, and no test staged on it can fail — which falsifies C23's stated justification for the widening (*"a preferences-only gate would have acked `projects-panel.tsx:208`'s write in flight"*): acking with that write in flight loses nothing. **The widening is right anyway**, and is proved for real by T015a's JSON-tab case — the preferences window is a separate **renderer process** with its own module instance, so *"name no windows, ask the list"* is load-bearing for **that** reason, not for this one. Recorded rather than dressed up: the task asked for a test that "fails for the right reason", and the honest finding is that this one cannot fail at all. Manufacturing an apparent failure here would have measured something other than the drain.
- [x] T016 [US2] Add `shutdownDrainTimeoutMs` to `IUiSettings` in `packages/core/src/config/settings.ts` and its documented default + env override in `packages/ui/src/main/ui-settings.ts` per [contracts/shutdown-drain.md](./contracts/shutdown-drain.md) §5 — `DEFAULT_SHUTDOWN_DRAIN_TIMEOUT_MS = 5000`, `numberFromEnv(env.THRONG_SHUTDOWN_DRAIN_TIMEOUT_MS, …)`, the same pattern as `DEFAULT_ATTACH_TIMEOUT_MS` (`ui-settings.ts:19`). The docblock MUST say what it is: a **backstop** for an unresponsive window, **never the mechanism** — the close waits on the **ack** (Principle X: injected from the composition root, never read from `process.env` inside a class).
- [x] T017 [US2] `packages/ui/src/renderer/state/workspace-store.tsx` — `flushSave` **returns** `client.save(...)` instead of dropping it with `void` (`:129`). A drain that is not awaited is not a drain (FR-010). Resolve on a no-op flush (nothing pending is a resolved promise, not a rejection); keep swallowing a save **failure** as today (`:128-130`) — a failed write must not wedge the close. **MUST NOT** change the debounce (`AUTOSAVE_DEBOUNCE_MS = 400`, `:42`), the project-switch flush or the unmount flush (`:135-164`) — those work.
- [x] T017a [US2] **GREEN — settle the write MODULE, not a list of components (C22/C23)**. Every config write already goes through **one chokepoint**: `writeConfig` in `packages/ui/src/renderer/config/write-config.ts`. **Do not count its callers** — how many there are is not a fact this design depends on, and every draft that stated a number was wrong (C19/C20/C21/C22, four times, the last of which cost real coverage). Put the drain at the chokepoint and the enumeration stops being needed at all. Three additions:
  1. **In-flight tracking — REUSE `writeChains`, do not add a set (C26).** `writeConfig` **already** keeps a module-level `Map<docKey, Promise>` of each doc's in-flight tail (`write-config.ts:24`, set at `:69`, deleted on settle when last-standing at `:74`) — chained per document because two writes to one file are not commutative (issue #50). `settleConfigWrites()` awaits `writeChains.values()`. **Adding a second copy of state the module already maintains would be a DRY violation in the story whose Constitution Check invokes Principle VIII** — and four drafts called this chokepoint "the story's only new construct" while half of it sat in the file they were all pointing at. What is genuinely new is the armed-**timer** registry, `scheduleWrite`, `cancelWrite` and `settleConfigWrites`. The chain map covers **every** write any window owns: debounced; **undebounced** (`keybindings-tab.tsx:117`, `preferences-app.tsx:176`'s `revertAll`, and `projects-panel.tsx:208` — in the **workspace** window, `app.tsx:493`); **awaited** ones too (`themes-tab.tsx:432`, `apply-client.ts:31`) because it never asked whether the caller dropped the promise; **orphaned** (below); and any writer added later — **without naming one of them**. Those are cited to show the *range*, not as a list to maintain: every draft that kept a list omitted something.
  2. **`scheduleWrite(id: ConfigDocId, produce: () => string | null, ms: number)`** and **`cancelWrite(id: ConfigDocId)`** in the same module — **debounced per `id`**, armed timers in a module-level registry, deregistered on fire or cancel. **No factory, no instance, no `useMemo` (C25).** The `produce` thunk runs **at fire time** and **`null` means "do not write" (C26)**.
     - `themes-tab.tsx` → **delete** the `writeTheme` `useMemo` + its unmount cleanup (`:295-302`) and call `scheduleWrite({kind:'theme', name: activeNameRef.current}, () => JSON.stringify(next), 150)` **from `applyTheme` (`:307`)** — the call site, which is where `activeNameRef` and `next` are in scope; `:295-302` no longer exists to host it. The id is correct **at call time**: `activeNameRef.current` *is* the captured-at-edit-time target, so **018 FR-023 holds by construction**.
     - `json-tab.tsx:54` → `scheduleWrite(docId, produce, 300)` where `produce` is **today's debounced body, moved unchanged**: parse; on failure `setInvalid(true)` and **`return null`** (007 FR-017 — the buffer is not applied; the last valid state stands); on success set `lastAppliedRef` to the **canonicalised** form (the watcher-echo suppression that stops the buffer being reflowed under the cursor), clear `dirtyRef`, `setExternal(null)` (007 FR-041), and return the string. **A `json: string` parameter would have forced all of this to the call site, per keystroke — breaking 007 FR-041's dirty/external logic and writing an unparseable buffer to the config file (C26).**
     - `json-tab.tsx:108` → `cancelWrite(docId)`, replacing `apply.cancel()` in `reload`. **Do not drop this**: it exists so *"a debounced apply of the edit we are abandoning must not fire afterwards and silently write it back over the document we just adopted"*. Without it, adopting an external change silently clobbers it — a silent config write-back, in the sweep against silent write-backs. Guarded green today by `preferences-json.e2e.ts` (T021a).
     **Why per-`id` and not `debouncedWrite(id, ms)`** (C22's original shape, which **cannot be implemented** — C25): `writeTheme` derives its id from the debounced **payload at fire time** and is memoised `[]`, one instance for every theme, so an id bound at creation cannot express it. **Per-`id` keying IS 018 FR-023's captured-at-edit-time guarantee** — theme A's pending write is keyed to A, theme B's to B, so neither displaces the other — now enforced by the module rather than by a comment and a payload convention. It also **dissolves** the orphaned-timer bug C21 spent an entry on: `json-tab`'s `useMemo(…, [docKey])` existed only to give each doc its own debounce, and minting a new one is what stranded the old one's armed timer; with per-id keying there is no instance to mint and no timer to strand.
     **NOT `apply-client.ts:24` — that debounce is unreachable dead code (C24).** `applyDebounced` (`:33`) has **zero callers** in src or tests; every apply-client write goes through `applyNow` (`:29-32`), which **cancels** the debounce and writes immediately (`settings-tab.tsx:119`, `themes-tab.tsx:312/404/438/449` — each `void`-dropped). **Delete `applyDebounced`, its debounce, and `createApplyClient`'s now-unused `debounceMs = 250` parameter** (`:23` — no caller passes it, and an unused parameter is a **lint error** under the v3.13.0 zero-error gate T062 enforces), **and with them `flush()` and `cancel()`** — whose bodies are nothing but `debounced.flush()`/`debounced.cancel()`, whose only `flush()` caller (`settings-tab.tsx:115`) this task deletes, and whose `cancel()` has no callers at all. **`ApplyClient` ends as `{ applyNow }` (C25)**; leaving the others is a **compile error**, not a smell, since `tsc -b` covers the renderer (`87e28a9`). YAGNI / Principle VIII, on T060's precedent. `applyNow`'s writes need no conversion at all: item (1) already covers them as undebounced dropped promises — and that the drain needs **no change** to cover them is the evidence this design is the right one.
  3. **`settleConfigWrites(): Promise<void>`** — flush every armed write, then await every in-flight promise. **This one function is the whole of what T019 calls.** Swallow write **failures** as T017 does; a failed write must not wedge the close.
  **Why not a per-tab provider** (the shape C20/C21 proposed, now superseded): `JsonTab` **does not unmount** on a tab switch. `preferences-app.tsx:273-288` renders `{mode === 'json' ? <JsonTab docId={…}/> : …}`, so in JSON mode a tab switch re-renders **the same instance** with a new `docId`; `apply` is `useMemo(…, [docKey])` (`json-tab.tsx:52-74`), so the change mints a **new** debounce and **orphans the old one's armed timer**, which still fires `void writeConfig(oldDocId, v)`. An unmount flush never runs on that path, and a registration keyed on mount would hold a **stale** `apply`. The module registry catches the orphan because the orphan is still registered. A design whose correctness depends on an accurate list — of components, or of windows — will be wrong again the next time someone adds one. This feature proved that repeatedly in one session: C19 miscounted writers; C20 modelled two of three tabs; C21 fixed an unmount that never happens; C22 gated the call on "a preferences window" while `projects-panel.tsx:208` writes from the workspace one (C23); and C24 found the correction counts false too.
  **Also delete** the two now-redundant cleanups `useEffect(() => () => x.flush(), [x])` (`settings-tab.tsx:115`, `themes-tab.tsx:302`) — the module settles them (Principle VIII).
  **MUST NOT** change the shared `debounce` helper's signature (`write-config.ts:93`): its **search-input** callers write nothing (`keybindings-tab.tsx:93`, `settings-tab.tsx:76`, `themes-tab.tsx:121`) and must not be dragged into a writer's fix — a blast radius bought for nothing is not a fix. `scheduleWrite` is built **on** it, for writers only. (No tally, per C24: after this task the only callers left are those search inputs.)
- [x] T018 [US2] Add the drain round-trip to `packages/ui/src/preload/preload.cts` and `packages/ui/src/renderer/global.d.ts`, beside the existing `appCloseChoice` (`global.d.ts:45`): a subscribe for `throng:appClose:drain` (`{requestId}`) and a send for `throng:appClose:drained` (`{requestId}`). **Correlated by `requestId`** so a stale ack cannot satisfy a later drain.
- [x] T019 [US2] The renderer's drain handler per [contracts/shutdown-drain.md](./contracts/shutdown-drain.md) §3: on `appClose:drain`, await **every deferred write the window owns** (**C19** — C6's reasoning one level down: a drain that reaches some writes and not others leaves the bug half-fixed for whoever edited the other one). `await Promise.all([flushSave(), settleConfigWrites()])` — in **every** window, **unconditionally** (**C22/C23**). **Do not enumerate tabs, writers or windows here.** `settleConfigWrites()` flushes every armed debounced write and awaits every in-flight one; the module knows what is pending, and enumeration is what every earlier draft got wrong (a miscount; a two-of-three tab model; an unmount that never happens; and — the one that cost real coverage — gating this call on *"in a preferences window"* while `projects-panel.tsx:208` writes `{kind:'settings'}` **from the workspace window**, `app.tsx:493`, which would have acked with that write in flight). A window with nothing pending settles immediately — correct, not a special case, and the same is true of a window that hosts no config writer at all. Then ack **once** with the same `requestId`, whether the writes succeeded or failed. **T017a is a hard precondition**: `settleConfigWrites` does not exist without it, and until it lands every flush returns `void`, so `await voidFn()` awaits `undefined` and this handler acks having drained nothing while T015a/T021 pass for the wrong reason. **MUST NOT** ack before the awaited writes settle, ack twice, or throw across the bridge.
- [x] T020 [US2] `packages/ui/src/main/main.ts` close handshake (`:650-686`) per [contracts/shutdown-drain.md](./contracts/shutdown-drain.md) §4: drain **before** allowing the close on **both** paths — the no-terminals path (`:657-665`, the one that loses the write today) **and** the `appClose:choice` handler (`:673-686`) for `'terminate'` **and** `'leave'` (the ones that survive by accident — the 2 green tests in the RED file exist to catch a fix that drains one exit and not the other). Drain **every** window (**C6**) from `BrowserWindow.getAllWindows()`, as `relaySync` already does (`:531-533`); `await Promise.all(...)` the acks, bounded by `shutdownDrainTimeoutMs` (a lapsed budget logs and closes anyway); skip a destroyed/crashed window rather than waiting out its budget. **The 250ms timer (`:661-663`) stops being a correctness device** — it may remain as the beat that lets the "Closing throng…" overlay paint, but the close now waits on the drain, not the clock. **Widening it to 500ms is the accident wearing a bigger coat and is explicitly refused (FR-011).** Do **not** move the drain to `will-quit`/`before-quit` — the renderer holding the write is already gone by then. Do **not** make the drain conditional on terminals, a prompt or a choice.
- [x] T021 [US2] Run `THRONG_E2E_RETRIES=0 npx playwright test terminate-all-drain.e2e.ts`: the 2 ordinary-close RED tests (`:259-311` layout, `:313-361` zoom) green; the 2 Terminate All tests (`:182-250` language override, `:370-424` layout+zoom) **still green** — the asymmetry is closed from both sides; and **all four new cases in this file green**: T015's sub-workspace drain (C6), T015a's preferences-window drain (C19), T015b's ordinary-close language override (the **only** measurement behind SC-005 / US2 AC4's "either exit" claim), and T015c's workspace-window config drain (C23's widening, which had no test at all until pass 19 asked). Also run `packages/ui/tests/e2e/app-close-terminals.e2e.ts` and `subworkspaces.e2e.ts` — the close handshake and the close-all cascade are exactly what they pin.
- [x] T021a [US2] **Run the neighbours T017a's blast radius threatens, and confirm they are still green** — every other story has this task; US2 went seven analysis passes without one, which is why C26's three defects were found by reading rather than by running. T017a rewires the **whole preferences write path** (`write-config.ts` — the chokepoint itself — plus `apply-client.ts`, `settings-tab.tsx`, `themes-tab.tsx`, `json-tab.tsx`). Run: `packages/ui/tests/unit/write-config-ordering.test.ts` (**the per-doc write chain, issue #50 — it guards the very function T017a modifies, and `writeChains` is what `settleConfigWrites` now awaits**), `packages/ui/tests/unit/debounce.test.ts` (the shared helper T017a must NOT change), and `packages/ui/tests/e2e/preferences-json.e2e.ts` (**the `reload` cancel — C26's silent-clobber case — and the invalid-JSON-not-applied case, 007 FR-017**), `preferences-settings.e2e.ts`, `preferences-themes.e2e.ts` (**018 FR-023's captured-at-edit-time guarantee**), `preferences-rapid-edit.e2e.ts` (debounce behaviour under fast typing) and `preferences-reset.e2e.ts`. **If `write-config-ordering` or `preferences-json` goes red, stop**: the chokepoint has broken the ordering guarantee or the abandon-edit path, and both are worse than the bug this story fixes.
- [x] T022 [US2] **REWRITTEN — its original premise was driven and found FALSE. The inverse is the real property, and is now a permanent guard.**
  **The premise, struck**: *"run with `THRONG_SHUTDOWN_DRAIN_TIMEOUT_MS=1`, make a layout change, close immediately — the change **must be lost** and the main log must say the budget lapsed. If it survives at a 1ms budget, the write is landing by luck and T021 passed for the wrong reason."*
  **Measured at a 1 ms budget, the layout change SURVIVES** (`npx playwright test terminate-all-drain.e2e.ts -g "a layout change made immediately before an ORDINARY close"` → `1 passed`). It is not landing by luck, and T021 did not pass for the wrong reason. The premise confuses the budget with the mechanism: the drain **REQUEST is sent regardless of the budget**, and the renderer flushes the moment it arrives. `shutdownDrainTimeoutMs` bounds only how long **main waits for the ack** — a lapsed budget stops main waiting, it does not **un-ask the question**, and the 250 ms overlay beat still leaves the already-fired flush time to land. **#86 was never "not enough time", it was "nobody asked"** — which is exactly why FR-011 refuses *widening* the clock as a fix, and why *shrinking* it is not a disproof. A task cannot demand the drain stop depending on a clock and then measure it with one.
  **The danger in leaving it as written**: a test built on that premise fails against correct code, and the cheapest way to "fix" it is to make correctness depend on the budget — reintroducing the very timing race this story removes.
  **The rewrite, landed** (`terminate-all-drain.e2e.ts`, *"#86 FR-011: a layout change survives an ORDINARY close even at a 1ms drain budget"*): the same journey at `THRONG_SHUTDOWN_DRAIN_TIMEOUT_MS=1`, asserting the layout **survives** — pinning contract §5's actual claim, that the budget is *"a **BACKSTOP** for an unresponsive window, **never the mechanism**"*. A hostile budget must not be able to lose a write. GREEN.
  **Not covered, and honestly out of reach here**: the backstop's *own* behaviour (a wedged renderer that never acks → main logs the lapse and closes anyway). It needs a renderer hung after the drain handler registers, which this harness cannot stage — the handler is wired at startup, before a test can reach in. The bound is implemented and code-reviewable (`main.ts` — `done('budget lapsed after …ms — closing anyway')`); it is the **absence of a hang** that is untested, not the absence of the timeout.

**Checkpoint**: both exits agree by construction. The only remaining loss is the documented one — ≤400ms under an **uncatchable** kill (FR-011), an accepted limit, not a defect.

---

## Phase 4: User Story 3 — A terminal that cannot start says so (#94, P2)

**Goal**: a launch that cannot complete surfaces as a **visible, actionable failure** within a bounded time, on any failure mode — and an elevated throng actually opens a working non-elevated terminal.

**Independent test**: `npx vitest run packages/daemon/tests/integration/pty-agent-launch-timeout.integration.test.ts` (~40s, elevation-free) — 2 RED go green.

- [x] T023 [US3] Add `agentConnectTimeoutMs` and `agentReadyTimeoutMs` to `IDaemonSettings` in `packages/core/src/config/settings.ts`, read in `readDaemonSettings` (`packages/daemon/src/composition-root.ts:70-74`) via the existing `numberFromEnv` — defaults **15000/15000**, env `THRONG_AGENT_CONNECT_TIMEOUT_MS` / `THRONG_AGENT_READY_TIMEOUT_MS` ([contracts/pty-agent-readiness.md](./contracts/pty-agent-readiness.md) §4). The budgets are **separate** (worst case 30s) so a slow connect does not consume the readiness allowance (**C7**).
- [x] T024 [US3] `packages/daemon/src/pty-agent-host.ts` — add `AgentBudgets` + an exported `DEFAULT_AGENT_BUDGETS`, taken as an **optional third constructor parameter**, and take `connectMs` from it in place of the hardcoded `Date.now() + 15_000` (`:36`, `:65`). This **removes** a hardcoded timeout from business logic rather than adding one (Principle X net-improved). **The 2-argument construction MUST keep compiling and working**: the RED test builds `new PtyAgentHost(pipe, () => { launches += 1 })` (`:96`, `:164`) and asserts the launch happened **synchronously in the constructor** (`:99`). A required third parameter would not compile there, and rewriting the RED test to fit the implementation inverts test-first. **MUST NOT** read `process.env` inside the class — production always injects from `packages/daemon/src/composition-root.ts`.
- [x] T025 [US3] `packages/daemon/src/pty-agent-host.ts` — **Route 1 (FR-012)**: when the connect deadline lapses, `sock.on('error')` currently retries `if (Date.now() < deadline)` and otherwise **does nothing** (`:67-74`). Call `failAllLive(reason)` (`:78-84`) — the failure path that already exists and already reaches the panel — **and give it the exit code as a parameter** — `failAllLive(reason: string, code: number | null)`, this lapse passing **`1`** (not today's unconditional `cb({ code: null })` at `:82`). **C27/C28**: four artifacts claimed it "already" emits non-zero; **it does not** — that is the `ev:'error'` branch (`cb({ code: 1 })`, `:127`), whose comment states what the non-zero code buys — *"so the Panel reverts with a visible message (005 FR-019), never a silent blank"* — which is exactly what FR-012 demands of *"a **visible**, actionable failure"*. `code: null` is the shape of an ordinary end, not a failure. Without this, Route 1 emits `null` while Route 2 (T026) is told to emit non-zero: two shapes for one outcome. **Nothing is weakened**: no test asserts `code: null`, and the RED test checks only `exits.length > 0` (`:114`, `:182`) — which is why nothing caught it. Pass the reason that names the failure **and appends the captured launch reason when one exists** (T030), e.g. `the de-elevated terminal agent never started: CreateProcessWithTokenW failed: 1314`. **Parameterise rather than change the body (C28)**: `failAllLive` has a **pre-existing caller** — `:63`, the `close` path, `failAllLive('the terminal agent stopped unexpectedly')` for a **crashed** agent. Changing the body to `code: 1` would change **that** path too, and it is user-visible (`terminal-panel.tsx:158` renders ``Terminal exited (code ${code ?? '—'})``): a crashed agent's panel would go from **"exited (code —)"** to **"exited (code 1)"**. That is a **regression in honesty** — when the agent crashes throng genuinely does not know the code, and `null`/"—" is the truthful answer. So `:63` passes **`null`** and is byte-for-byte unchanged. *(C27's "this weakens no test" was true and verified; "weakens no test" is not "changes no behaviour".)*
  **Do NOT relaunch (C8)**: the `close`-path relaunch — **`:64-65`**, *not* `:63-65`, which is the anchor an earlier draft used and which quietly included the `failAllLive` call this task modifies — stays exactly as it is. A *crashed* agent may come back; one that never arrived indicates the shim, and relaunching produces a launch loop that buries the error. Leave `dispose()`'s suppression intact (`:188`) — a deliberate close must not fire a failure. **Fail the STARTED keys, not merely the keys that happen to have listeners registered**: `failAllLive` iterates `dataCbs`/`exitCbs` keys, and the RED test registers `onData`/`onExit` **after** `start()` (`:101-104`) — the lapse fires long after registration there, but a fix that depends on that ordering is a race, not a fix.
- [x] T026 [US3] `packages/daemon/src/pty-agent-host.ts` — **Route 2 (FR-013)**: handle the readiness ack the protocol has always defined and the host has always discarded (`case 'ready': case 'started': break;` at `:133-135` ← `pty-agent-protocol.ts:26`). Start a **per-key** `readyMs` timer **at connect** (**C7** — not at `start()`: before connect there is no agent that could ack); clear it on `{ev:'started', key}`; on lapse fail **that key only** (`[throng] the terminal never started` + a non-zero exit), leaving other live terminals alone; clear it on `exit`/`error`/`forgetKey` (`:86-91`) so a key that ended cannot re-fire. **MUST NOT** treat **first output** as readiness — a legitimately slow shell that has printed nothing is still starting, and killing it is US3 AC4's explicit prohibition — and MUST NOT share one clock across keys.
- [x] T027 [US3] `packages/daemon/src/pty-agent-entry.ts` — **verified during analysis: the ack already exists.** `:91` emits `send({ ev: 'started', key: msg.key, pid: h.pid })` the moment the ConPTY is spawned (and `:130` sends `{ev:'ready'}` for the connection itself). **So this task is a confirmation, not a build**: re-read `:91`, confirm it fires on **every** start path and is not conditional, and change **nothing** if so. This resolves what was an open unknown at planning time — T026 hard-depends on it (a readiness budget waiting on an ack no agent sends fails **every** terminal), so it was scoped as "verify, then implement if absent"; the verification is done and the answer is *present*. **Only if a start path is found that does not ack** does this become protocol work — in which case add the emit at that path and nowhere else. The RED test's second scenario stands up a live server that accepts the `start` and **deliberately never replies** (`:139-158`), so it proves the host waits for an **ack** rather than for silence to end — it does **not** depend on the real agent's emit, which is why the two can be verified independently.
- [x] T028 [US3] Widen the launch seam **additively** so a failure is reportable (FR-015): `launch: (pipeName: string, report: (reason: string) => void) => void` in `packages/daemon/src/pty-agent-host.ts`, and pass `report` down from `packages/daemon/src/composition-root.ts:140-148` — to `deElevatedLauncher.launch(...)` on the elevated branch **and** to the plain `spawn` branch (`stdio: ['ignore','ignore','pipe']`, report a non-zero exit the same way). `report` **must** arrive as a **parameter of the callback**, never a closure over `deElevatedPty`: the host's constructor invokes `launch` **synchronously** (`:35`), before `deElevatedPty` is assigned — a closure reference is a temporal dead zone. Store the last reason on the host and append it to T025's message. Do **not** make `launch` async: `IPtyHost.start` is synchronous by contract.
- [x] T029 [P] [US3] **RED**: new contract test `packages/platform-windows/tests/contract/windows-de-elevated-launcher.contract.test.ts` — a shim that exits non-zero with a message on stderr causes `report(stderr)` to be called with **that message**; a zero exit reports nothing; `launch` still **returns `void`** and does not block or throw. This is the requirement's whole purpose: the shim already throws precise, actionable messages (`$ErrorActionPreference='Stop'`, `"CreateProcessWithTokenW failed: <win32 error>"`) **straight into `/dev/null`**, which is why nobody has ever known why #94 hangs. Run it; confirm it fails because `report` does not exist.
- [x] T030 [US3] **GREEN**: `packages/platform-windows/src/windows-de-elevated-launcher.ts` — swap `stdio:'ignore'` (`:31-34`) for `stdio: ['ignore','ignore','pipe']`, collect stderr, and call `report(stderr || \`exit ${code}\`)` on a non-zero exit. **Keep** `.unref()` and `windowsHide` — the launch stays fire-and-forget in *lifetime*; only its **failure** becomes observable — and keep it synchronous, returning `void`, behind its Principle II seam. **MUST NOT** pipe stdout (the agent's output belongs on the pipe), block on the shim, or throw.
- [x] T031 [US3] **Close the CI hole (FR-013a / SC-008)** in `.github/workflows/ci.yml`: add a step running the admin subset **on the elevated runner** with `THRONG_E2E_INCLUDE_ADMIN=1 npx playwright test --grep @admin` **directly** — **not** `npm run test:e2e:admin`, which exists to hop UAC from a non-elevated shell (`scripts/test-e2e-admin.mjs:20-24`, `Start-Process -Verb RunAs`) and is pointless on a runner that is already elevated. The step **MUST fail on zero executed tests**: Playwright exits **0 on an empty selection**, which is exactly how this hole stayed invisible — SC-008 measures *executed > 0*, not *exit 0*. In the same change, fix **two comments, for two different reasons** — do not conflate them: (a) `packages/ui/tests/e2e/admin.ts`'s `skipIfElevated` docblock (`:49`) is **false today** — it asserts *"CI runs non-elevated so these still execute there"* when GitHub's Windows runners are elevated and those specs therefore self-skip; correct it to say they are verified on a **developer's non-elevated run**, not in CI. (b) `ci.yml`'s comment block at `:157-166` needs updating for **two different reasons, so read to the end of it**: `:157-160` (*"the runner is elevated … @admin specs remain excluded by default … non-elevated-baseline specs self-skip"*) is **accurate today** — **do not "correct" it and do not go hunting for a contradiction in it**; it is *this task's own new step* that makes its "excluded by default" clause stale, so update it to describe the step you just added. But `:161-166` **is** false today: it claims the elevated de-elevation path "is now handled by the environment guard … that routes it to **the dedicated elevated runner, where it can actually be verified**" — there is no such runner or step, which is the whole of FR-013a. That sentence describes the thing you are about to build as though it already exists, and it is why the hole read as covered. The spec's correction (`admin.ts`'s docblock asserts the opposite of what `ci.yml:157-158` does) names `ci.yml` as the **truthful** side of that pair. Today: `playwright.config.ts:55,88` `grepInvert`s `@admin` unless `THRONG_E2E_INCLUDE_ADMIN` is set, and CI never sets it — so `@admin` specs are excluded from the **only** runner capable of executing them.
- [x] T031a [US3] **Forward the new budgets across the UAC hop**, or T033's second half is an inert proof: append `THRONG_AGENT_CONNECT_TIMEOUT_MS` and `THRONG_AGENT_READY_TIMEOUT_MS` (both **new in T023**) to the `forwarded` allow-list in `scripts/test-e2e-admin.mjs:37`. `Start-Process -Verb RunAs` launches a new process that **does NOT inherit the caller's environment block** — the script's own docblock memorialises this, having been bitten by exactly it: `THRONG_E2E_RETRIES=0 npm run test:e2e:admin` *"LOOKED like a retries-disabled run"* while the elevated suite *"quietly executed at the config default"*. Only three vars are forwarded today. Without this, T033's `$env:THRONG_AGENT_CONNECT_TIMEOUT_MS='2000'` never arrives, the run silently uses the 15s default, and the step passes without testing anything — **the same inert-proof class T034 was rewritten to demolish**. Shipping it inside a feature whose stated purpose is closing "a proof that cannot fail" would be indefensible. **Blocks T033.**
- [x] T032 [US3] Run `npx vitest run packages/daemon/tests/integration/pty-agent-launch-timeout.integration.test.ts` — both RED green (~40s: each scenario deliberately waits past the 15s budget + a 3s grace, so **each budget must lapse into a failure on its own**). Then confirm the neighbours are still green: `packages/daemon/tests/integration/pty-agent-host.integration.test.ts`, `terminal-service.integration.test.ts`, `terminal-hygiene.integration.test.ts`, `terminal-root-lock.integration.test.ts` — `failAllLive` is also what releases the project-root lock (Principle III).
- [ ] T033 [US3] **Run the half no CI has ever run** ([quickstart.md](./quickstart.md) §#94), from an **elevated** PowerShell, **with budgets that fit the test's own deadline (C27)**: `$env:THRONG_AGENT_CONNECT_TIMEOUT_MS='8000'; $env:THRONG_AGENT_READY_TIMEOUT_MS='8000'; npm run test:e2e:admin -- terminal-de-elevation-hang.e2e.ts`. **Why**: the spec's worst case is connect 15s + readiness 15s = **30s** (C7), but the test's own `LAUNCH_BUDGET_MS` is **25s** (`terminal-de-elevation-hang.e2e.ts:33`) and on lapse it reports *"it hung (#94)"* — **the very failure it exists to detect**. A connect at ~14s followed by no ack would surface at ~29s and be reported as a hang. 8000/8000 makes the worst case 16s. **This needs no test change** (T054 is the one sanctioned amendment): the budgets are injectable because C7/T023 made them so, and **T031a is what carries them across the UAC hop** — without T031a they silently do not arrive and the run is back at 15/15. Production defaults stay 15/15. Three outcomes, and **only one is done**: a working non-elevated prompt = FR-014 met, ship; a visible `[throng] …` error within **~16s** (the budget this task injects; ~30s at production defaults) = FR-012 met but **FR-014 unmet** — de-elevation is genuinely broken, and FR-015's captured reason now says **why**; a blank terminal still "running" = **#94 is not fixed**. The test itself refuses to pass on a visible error (`:109-112`), and that refusal is deliberate: **passing silently on `outcome === 'visible-error'` is forbidden**. Also prove the reason is real rather than generic: `$env:THRONG_AGENT_CONNECT_TIMEOUT_MS='2000'` and read the terminal's text — it must name the **shim's own** error. **T031a must have landed first**: `Start-Process -Verb RunAs` does not inherit the caller's environment, so until the var is on `test-e2e-admin.mjs`'s `forwarded` allow-list this step runs at the 15s default and proves nothing while looking like it proved something.
- [x] T034 [US3] **RED-then-GREEN: prove a legitimately slow shell is not killed (US3 AC4) against the code that could kill it.** Add a third scenario to `packages/daemon/tests/integration/pty-agent-launch-timeout.integration.test.ts`, beside the two RED ones: a live server that accepts the `start`, **acks `{ev:'started', key, pid}`**, and then emits **no output at all** for longer than `agentReadyTimeoutMs` — assert **no** `onExit` and **no** `[throng]` chunk. Run it before T026 (it passes vacuously — nothing kills anything yet) and after (it must still pass: T026's budget is cleared by the **ack**, so silence after the ack is none of its business). Inject a short `THRONG_AGENT_READY_TIMEOUT_MS` so it costs ~2s, not 15.
  **Why not `terminal-slow-start.e2e.ts`, which an earlier draft of this task named**: that spec **cannot fail** for this reason and would have reported a green pass on an untested path. It (a) calls `skipIfElevated()` (`:21`) and (b) launches with `env: { THRONG_ATTACH_TIMEOUT_MS, THRONG_ATTACH_DELAY_MS }` **hard-coded at `:70`** and no `THRONG_FORCE_PTY_AGENT` — so a shell prefix of `THRONG_ATTACH_DELAY_MS=8000` does nothing, and a **non-elevated daemon routes terminals to `NodePtyHost`**: `PtyAgentHost`, and therefore T026's readiness budget, is **never in the path**. `THRONG_ATTACH_DELAY_MS` delays `TerminalService`'s cold-start attach — an unrelated mechanism one layer up. The test passes identically whether or not the budget kills slow shells, which is the definition of an inert proof.
  Still run `npx playwright test terminal-slow-start.e2e.ts` **as a neighbour** (unchanged — it guards the "still starting" retry, which is real coverage of a different thing), and, if you want the E2E-level version of AC4, `THRONG_FORCE_PTY_AGENT=1` is the existing hook that forces every terminal through the agent at medium integrity (`composition-root.ts:150-151`, a seam that exists for exactly this) — but the integration scenario above is the one that owns the assertion, because it drives `PtyAgentHost` directly and runs in CI.

**Checkpoint**: no launch failure is silent, and the `@admin` suite finally executes somewhere. If T033 says FR-014 is unmet, **split it out as its own issue with the captured reason attached** and ship FR-012's bounded failure — an honest partial delivery, not a laundered pass.

---

## Phase 5: User Story 4 — The terminal-flavour settings can actually be edited (#67, P2)

**Goal**: both settings get a control that fits their shape — a multi-select over the **detected** built-ins, and a structured record table with per-field validation — built by **generalising** the one table, never by adding a second (FR-020).

**Independent test**: `THRONG_E2E_RETRIES=0 npx playwright test preferences-terminal-flavours.e2e.ts` — 4 RED go green.

- [x] T035 [P] [US4] **RED**: new unit test `packages/core/tests/unit/flavour-record.test.ts` for `validateFlavourRecord(record, existingIds)` per [contracts/settings-descriptors.md](./contracts/settings-descriptors.md) §5 — it returns a **message, not a boolean** (the idiom `validateKey` established at `map-control.tsx:38-55`; "invalid" with no reason is a dead end for a user who cannot see the rule), and the RED E2E asserts on the message text: a missing id → contains `id`; a duplicate id → contains `already`; a missing executable → contains `executable`; a valid record → `null`. **C12**: an executable means **non-empty, NOT existence-checked** — a path may be valid on another machine or not installed yet, and launch already reports *"not available on this machine"*, which is the right place for that check. Run it; confirm it fails because the module does not exist.
- [x] T036 [US4] **GREEN**: implement `packages/core/src/terminal/flavour-record.ts` — pure — and export it from the `@throng/core` barrel.
- [x] T036a [P] [US4] **RED**: add to `packages/core/tests/unit/metadata.test.ts` the assertion that **`emptyValueFor({control:'records', …})` returns `[]`**, not `''`. This is **016's F6 bug about to be re-created by the fix for it**, and nothing existing can see it: `emptyValueFor` (`metadata.ts:115-123`) special-cases `map → {}` and `array`/`multiselect` → `[]`, and **everything else falls through to `''`** — so a `records` field's clear affordance (`settings-tab.tsx` commits `emptyValueFor(d)`) would write `""` into an array-valued setting. `auditClearable` (`clearable.test.ts:82`) **cannot catch it**: `parseTerminals` coerces `''` back to `[...fallback.flavours]` = `[]`, `isEmptyValue` sees an empty array and reports success — the audit passes while the document is wrong. That is precisely the "declaration that is a lie" the audit exists to prevent, sailing straight past it. `isEmptyValue` needs **no** new arm (its `Array.isArray(value)` branch at `:130` already covers records) — assert that too, so a future reader does not add a redundant one. Run it; confirm it fails with `''`.
- [x] T037 [US4] `packages/core/src/config/metadata.ts` — add `ControlKind = … | 'records'`, the `records`-only `FieldDescriptor` fields `idKey` (the property that identifies a row, **immutable once created** — C13) and `itemNoun` (what one row **is**; it labels the affordances and names the test ids), **and the `emptyValueFor` arm T036a demands: `records` empties to `[]`** (extend the existing `map` special-case; keep its comment's reasoning, which is the same reasoning one control-kind over). **A new `ControlKind` is not just a type widening** — it silently opts out of every control-kind switch in the fleet: `emptyValueFor` here (T036a) and `renderControl`'s dispatch (T042). Both must gain an arm in this feature or the descriptor is valid, the audit is green, and the user sees nonsense. **The mode is DECLARED, never inferred from the value (FR-018)**: today `control:'array'` lets the control type **flip with the data** — `[].every(...)` is vacuously true, so an *empty* `terminals.flavours` renders as a string-array control whose Add appends `''` (an empty **string** into an array of **objects**, which the tolerant parser then drops), and one entry later it is a JSON textarea.
- [x] T038 [US4] `packages/core/src/config/settings-metadata.ts` — the descriptor deltas per [contracts/settings-descriptors.md](./contracts/settings-descriptors.md) §3: `terminals.flavours` (`:159-166`) becomes `control: 'records', idKey: 'id', itemNoun: 'flavour', **clearable: true**` — a **deliberate new affordance**, not a restoration: `terminals.flavours` declares **no** `clearable` today, and `clearable` is opt-in (`settings-tab.tsx:152`, `if (!d.clearable) return false`). It is added because contract §3 declares it and because both siblings declare it for exactly this reason — `disabledBuiltins` (*"empty = hide nothing, which is also what it ships as"*) and `defaultParams` (*"ships empty; empty means pass nothing extra, a perfectly good answer"*) — and an empty custom-flavour list is the same kind of perfectly good answer. *(An earlier draft justified it as recovering an affordance the setting "has today"; it has no such affordance, and the addition stands on the sibling precedent instead.)* With columns `label` / `file` / `args` / `defaultParams`, all `control: 'text'`; **T037's `emptyValueFor` arm is a hard precondition of `clearable: true`** — declaring a field clearable whose empty value is `''` is exactly the lie `auditClearable` exists to catch and, per T036a, the one shape it cannot see; `terminals.disabledBuiltins` (`:167-175`) becomes `control: 'multiselect'` with **dynamic** options (007 FR-029: never a free-text field where an enumeration exists). Leave `terminals.defaultParams`'s descriptor (`:185-186`) **unchanged** — it was always right; the **cell** was missing (T041).
- [x] T039 [US4] **Update the test that encodes the bug**: `packages/core/tests/unit/settings-metadata.test.ts:86` asserts `terminals.disabledBuiltins`'s control **is `'array'`** — the defect. It must assert `'multiselect'` once T038 lands, and any sibling assertion pinning `terminals.flavours` as `'array'` must assert `'records'`. Also check `packages/core/tests/unit/app-settings.terminals.test.ts` for the same shape. **This is an assertion that pins today's defect, not a guard being weakened** — the distinction matters, and no other assertion in these files may move.
- [x] T040 [P] [US4] **RED**: add the **C14 regression test** for `terminals.defaultParams` to `packages/ui/tests/e2e/preferences-map-control.e2e.ts` — the "Default flavour parameters" control's cell renders an `input[type="text"]` (**not** an empty `<select>`), accepts a typed value, and the value lands in `settings.json`. **It has zero test coverage today**, so the test **is** the deliverable that makes the fix real (C14 folds this in rather than raising an issue, because it renders an empty `<select>` purely for the missing text cell that FR-018 must add anyway — `settings-metadata.ts:185-186` + `map-control.tsx:264-277`). Run it; confirm it fails on the empty `<select>`.
- [x] T041 [US4] **GREEN**: the text cell — `MapCell` (`packages/ui/src/renderer/preferences/map-control.tsx:237-278`) renders `control: 'text'` as `<input type="text">`. Today anything that is not `control:'number'` falls through to a `<select>` over `allowedValues ?? options ?? []` (`:264-277`). This one arm repairs `terminals.defaultParams` (C14, T040) and is a precondition for T042's flavour table.
- [x] T041a [P] [US4] **RED**: the **C17 duplicate-id-from-file** case, added to `packages/ui/tests/e2e/preferences-terminal-flavours.e2e.ts` — seed `THRONG_CONFIG_ROOT`'s `settings.json` by hand with two flavours claiming **one** id (reachable today: the JSON tab ships and `parseTerminals` does not dedupe), open Preferences, and assert **both rows render, in file order** — addressed by **`rowKey`** ([contracts/settings-descriptors.md](./contracts/settings-descriptors.md) §2): the first is `flavour-row-<id>`, the second `flavour-row-<id>-1` — and the second carries the "already" message in its **own** `flavour-row-error-<id>-1` cell. **Use those locators, not `flavour-error`**: the control-level region is transient add/commit state and says nothing about a row that arrived pre-broken, and an id-derived row id would put two elements behind one test id (a Playwright strict-mode violation — the duplicate case would be untestable, i.e. undefined behaviour with extra steps). **Neither row may vanish**: a control that silently drops a row the user's own file contains is the same defect as #67's tolerant parser swallowing the `''` its Add button appended, and shipping it inside the fix for that bug would be indefensible. Run it; confirm it fails (today `asMap` rejects the array outright and renders an empty table). C17 exists because C9 claimed `dedupeById` guaranteed uniqueness here and it does not.
- [x] T042 [US4] **GREEN**: **first, route the control at all** — add `case 'records': return <MapControl {...props} />;` to `renderControl` in `packages/ui/src/renderer/preferences/form-controls.tsx` (beside `case 'map'`, `:48`). **Without this arm the descriptor falls to the `default:` arm and renders as a TEXT FIELD showing `[object Object]`** — not a crash, not a type error, caught by nothing, because the descriptor is valid and the control is valid. That exact degradation is what the comment at `:43-47` was written to memorialise when 016 hit it, and this feature would walk into it again: T038's descriptor delta alone makes `terminals.flavours` a text field. Then the `records` mode of `packages/ui/src/renderer/preferences/map-control.tsx` per [contracts/settings-descriptors.md](./contracts/settings-descriptors.md) §2 — **one component, two modes; NOT a second table (FR-020/C9)**. An **array-aware reader** (`asMap` at `:29-30` rejects arrays outright, which is why feeding it `terminals.flavours` today renders an empty table and **the first commit overwrites the array with an object**); rows carrying `record[idKey]` but **keyed by index, not by id (C17)** — `dedupeById` (`flavour.ts:55`) does **not** make ids unique here: it is not exported, runs on the merged *runtime* list, and shapes only the **launch list**, while `parseTerminals` does not dedupe `terminals.flavours` and the JSON tab ships. A file-authored duplicate id therefore **renders every row in file order** and **flags the offending row** with `validateFlavourRecord`'s "already" message (T041a) — **never** silently dropped or auto-deduped, which would be this feature committing the silent-drop defect it exists to fix; the index is the honest key precisely because order is preserved; **order preserved, never sorted (C11)** — it is the panel dropdown's order and `mergeFlavours` is first-wins, while the `map` mode keeps its alphabetical sort where order is meaningless; the id column rendered as **text and not editable (C13)** — it keys `terminals.defaultParams`, and to rename you delete and re-add; commit `[...records]` with the row replaced, **never** an object; refuse an invalid commit **in the editor with a reason** via the existing error region (`:208-212`) and `validateFlavourRecord`; **add the per-row error cell `${itemNoun}-row-error-${rowKey}` (C17/T041a)** — rendered only when that row fails validation, because the existing `${itemNoun}-error` region is driven by **transient add/commit state** and structurally cannot speak for a row that arrived pre-broken from the file; it keeps its current job and test id **unchanged** (the RED E2E asserts on it at `:198`/`:205`/`:215`); add/remove stay on `IconButton` with hover titles exactly as `:143-148` / `:201-206` (constitution v3.12.0, non-negotiable); the flavour **error** region is a `role="alert"` message, not a control. Test ids: the `records` mode derives `${itemNoun}-row-${rowKey}`, `${itemNoun}-cell-${rowKey}-${column.key}`, `${itemNoun}-row-error-${rowKey}`, `${itemNoun}-new-id`, `${itemNoun}-add`, `${itemNoun}-error` — where **`rowKey` is the row's `id` for the first row claiming it and `${id}-${index}` thereafter (C17)**, so for the unique ids the RED E2E drives `rowKey === id` and the scheme it demands (`:175-215`) is **exactly** what ships; the suffix appears only for a file-authored duplicate, where an id-derived id would put two elements behind one test id — while the `map` mode keeps `map-*-${descriptor.key}` **untouched**, because 016's E2E depends on it and renaming a green suite's ids buys a consistency no user can see.
- [x] T043 [US4] **GREEN**: `args` is `string[]` and the RED test does not drive it — render it as a text cell over a **space-separated round-trip** (parse on commit, join on render), the same thing the Startup Params field already asks a user to type. A nested array editor is a capability nobody asked for (YAGNI, Principle VIII).
- [x] T044 [US4] **GREEN**: the raw detected set on its **own channel (C10 / FR-017)** — `listDetectedFlavours(): Promise<DetectedFlavour[]>` in `packages/ui/src/main/shell-detection-service.ts` returning the built-ins as `detectInstalledShells()` reported them, **with nothing subtracted**; the IPC handler `throng:terminal:listDetectedFlavours` in `packages/ui/src/main/main.ts` beside `:595`; the channel in `packages/ui/src/preload/preload.cts` + `packages/ui/src/renderer/global.d.ts`. `listFlavours()` — the **panel dropdown's** source, which subtracts `disabledBuiltins` (`:27-35` → `flavour.ts:33,42-43`) — is **unchanged**. **Why a method and not `listFlavours({includeHidden:true})`**: a flag invites a caller to accidentally offer hidden flavours in the dropdown itself — the exact behaviour the user switched off. Re-detect per call as `listFlavours` already does (`:26-32`), and keep it in **UI main** (detection is a Principle II seam; the daemon does none; the renderer is sandboxed). It **MUST NOT** be reachable from the panel-type form.
- [x] T045 [US4] **GREEN**: feed the picker from the existing dynamic-options seam in `packages/ui/src/renderer/preferences/settings-tab.tsx:135-145` — the hook 016 generalised for precisely this — fetching the detected ids once, as themes already are (`:88-104`). Every detected built-in is offered **including the ones currently hidden, shown checked (FR-017)** — the catalogue is the **detected** set, never the visible set, or hiding is a one-way door. Each option is **labelled** with the flavour's human label: the RED test finds `cmd` by label text matching `/cmd|Command Prompt/i`, **never by position**, because "the first checkbox" would pass against a picker offering the wrong set entirely (`:125-129`). An empty catalogue renders an **empty picker, never a text box** — the RED test enumerates the control's root **and** its descendants and asserts **zero** `TEXTAREA`/`INPUT:text` (`:68-101`).
- [x] T046 [US4] Run `THRONG_E2E_RETRIES=0 npx playwright test preferences-terminal-flavours.e2e.ts` — all 4 RED green.
- [x] T047 [US4] Run the green callers this blast radius threatens and confirm they are **still green** (FR-020 forbids a second table precisely so this list stays short): `packages/ui/tests/e2e/preferences-map-control.e2e.ts` (016's keyed-map E2E — `editor.indentByLanguage`, `editor.languageByExtension`), `terminal-flavours.e2e.ts`, `preferences-settings.e2e.ts`, `preferences-json.e2e.ts`, `preferences-reset.e2e.ts`, plus `packages/core/tests/unit/app-settings.terminals.test.ts`, `terminal-flavour.test.ts` and `packages/ui/tests/unit/panel-type-form.test.ts`. Confirm `assertEveryKeyDescribed` still passes (constitution v3.11.0).

**Checkpoint**: neither flavour setting presents free text or JSON in **any** state, including empty; a hidden built-in can be un-hidden through the editor alone; and `terminals.defaultParams` is editable for the first time.

---

## Phase 6: User Story 5 — Every setting shown does something (#95, P3)

**Goal**: exactly one setting governs file-tree open-on-click, it lives where users look for it, and no rendered control governs nothing.

**Independent test**: `npx vitest run packages/core/tests/unit/settings-open-on-click-single-owner.test.ts` — 2 RED go green.

- [x] T048 [P] [US5] **RED**: add the group-move coverage to `packages/ui/tests/e2e/preferences-settings.e2e.ts` — the constitution requires E2E for a UI-visible change, and the guard test (which is pure) cannot see **discoverability**, which is the whole of C2. Assert: the **File Explorer** group shows *"Open files with"*; there is **no** second open-on-click control anywhere in Preferences (FR-021); its allowed values include **`none`**, which C2/FR-024 retains and makes visible for the first time; and a hand-written `"explorer": { "openMode": "double" }` in the seeded `THRONG_CONFIG_ROOT` `settings.json` changes **nothing** and raises **no warning** (C1/FR-023 — the key is ignored on load and stripped on the next write). Run it; confirm it fails against today's grouping.
- [x] T049 [US5] `packages/core/src/config/app-settings.ts` — delete `explorer.openMode` **entirely and together**: the `ExplorerSettings.openMode` field (`:21`), the `DEFAULT_APP_SETTINGS.explorer.openMode` default (`:199`), the `explorerSettings(...)` parse branch (`:269-270`) and the `cloneExplorer(...)` copy (`:291`). **Dropped, not migrated (C1/FR-023)**: the values are not compatible (`editor.openOnClick` has `none`; `openMode` does not), so a migration must invent an intent the user never expressed — and since `explorer.openMode` has **never had any effect**, a user with `openMode:'double'` gets single-click **today**; migrating would **change** their behaviour, while dropping preserves exactly what they have. The deletion **is** the mechanism: the existing tolerant parse already drops unknown keys silently (`:267-285`), so it is idempotent by construction (v3.5.0). **MUST NOT** migrate, reinterpret or warn.
- [x] T050 [US5] `packages/core/src/config/settings-metadata.ts` — delete the `explorer.openMode` descriptor, and move the survivor: `editor.openOnClick` keeps its **key** (no rename, so **no migration of a setting that works**) and gains `group: 'File Explorer'`, `label: 'Open files with'`, keeping `allowedValues: ['single','double','none']`. A descriptor's Preferences section comes from its explicit `group` field (`metadata.ts:65`), **not** from its key prefix — which is what makes C2 possible at all. Deleting the key and its descriptor **together** keeps `assertEveryKeyDescribed` satisfied in both directions (v3.11.0).
- [x] T051 [US5] `packages/core/src/explorer/open-intent.ts:17` — rename `decideClick`'s parameter from `openMode` to **`openOnClick`** and retype it from `OpenMode` to `EditorOpenOnClick` (`'single'|'double'|'none'`), the type it is **actually fed** (`file-tree.tsx:270` → `tree-node.tsx:47-49`, where `none` is already handled by an early return, so behaviour is unchanged). **That parameter name is literally how #95 hid**: a search for `explorer.openMode`'s consumer appears to find one. Dispose of the now-userless `OpenMode` type (`app-settings.ts:14`) accordingly. This is what makes the guard test pass **structurally** rather than accidentally.
- [x] T052 [US5] **Update the tests that encode the bug** — each asserts the defect and must move with the fix: `packages/core/tests/unit/app-settings.explorer.test.ts:7` (defaults to `openMode: 'single'`), `:19` (coerces an invalid `openMode` back), `:25` (accepts a custom `openMode`) — drop the `openMode` assertions while **keeping** every `deleteMode`/`excludeGlobs` assertion in the same tests intact; `packages/core/tests/unit/metadata.test.ts:44` (`leaves` contains `explorer.openMode`); `packages/core/tests/unit/settings-metadata.test.ts:62` (`explorer.openMode.allowedValues`). Check `packages/core/tests/unit/editor-settings.test.ts` and any `open-intent` test for an `OpenMode` import. **Nothing else in these files may change** — they are guards for settings that work.
- [x] T053 [US5] Run `npx vitest run packages/core/tests/unit/settings-open-on-click-single-owner.test.ts` — both RED green: exactly one claimant is rendered, and it has readers **outside the config layer** (the test excludes `app-settings.ts`/`settings-metadata.ts`/`metadata.ts` at `:41`/`:50` precisely because a mention there proves only that a setting **exists**). Then confirm the behaviour guards are still green: `packages/ui/tests/e2e/explorer.e2e.ts`, `editor-open.e2e.ts` and T048's new coverage.

**Checkpoint**: one owner per behaviour; a control the user changes does something.

---

## Phase 7: User Story 6 — A theme cannot ship code you cannot read (#83, P3)

**Goal**: every colour the editor paints code with is measured against the background it is painted on, from a pairing list **derived** from the token set — so a token that does not exist yet is measured anyway.

**Independent test**: `npx vitest run packages/core/tests/unit/theme-syntax-body-contrast.test.ts` — 4 RED go green, the 16 green stay green.

**No theme is recoloured.** #83's premise is **false** — 150 pairs, zero failures, every pair ≥6.01:1 (`Light`'s worst is 6.12). The **guard gap** is entirely real, and `THRONG_THEME` — hand-authored, bypassing `makeTheme`'s lift — currently passes **by luck**.

- [x] T054 [US6] **The ONE sanctioned test amendment in this feature.** `packages/core/tests/unit/theme-syntax-body-contrast.test.ts:65` asserts `pairing.min` **`toBe(WCAG_AA_BODY)`** (4.5) while **C3 settles the gate at 6.0**; both cannot hold. The test **predates** the clarification session and its own stated intent is *"at the body-text threshold, **not a relaxed UI one**"* — it defends against `WCAG_AA_LARGE_UI` (3.0), and 6.0 satisfies that intent **more strictly**. Amend that single assertion to `toBe(SYNTAX_BODY_MIN)` and add `expect(SYNTAX_BODY_MIN).toBeGreaterThanOrEqual(WCAG_AA_BODY)` beside it, so the "not relaxed" property is still asserted and the house standard is stated. **Nothing else in the file changes** — the 16 green assertions gate the 4.5 **floor** and pass at 6.01+. **The alternative — implementing 4.5 and calling C3 satisfied — is refused**: the 6:1 lift in `makeTheme` stays either way, and a gate below it protects nothing. If the owner would rather keep the test literal, the correct move is to **overturn C3**, not to quietly ship 4.5.
- [x] T055 [US6] **GREEN**: `packages/core/src/config/theme-quality.ts` per [contracts/theme-contrast.md](./contracts/theme-contrast.md) §1 — **derive twice**. `SYNTAX_TOKENS` (today a hand-list at `:292-303` — the same bug one level down, which is why the RED test refuses to trust it and derives its own set at `:45-48`/`:106-108`) becomes `Object.keys(THRONG_THEME.colours).filter(t => t.startsWith('syntax')).sort()`; `SYNTAX_BODY_MIN = 6.0` with **its reasoning recorded in the source, not only in a spec** (WCAG AA 4.5 is the **floor**; `makeTheme` lifts every seed to 6:1 at `default-themes/index.ts:186-192` because the search-match tint can only be as strong as the weakest syntax hue permits, so a 4.5 gate would be **weaker than the derivation it exists to protect** — it would admit a comment authored at exactly 4.5 that collapses the search highlight to invisibility); `BY_DESIGN_LOW_CONTRAST_THEMES = ['Matrix','VI-VIM','Gothic']` (#61's policy, FR-028); `SYNTAX_ON_BODY` derived from `SYNTAX_TOKENS` and appended to `CONTRAST_PAIRINGS`. Measure the **shipped** token `theme.colours[t]`, **never a seed** (FR-029 — the authored seeds are not what ships). **MUST NOT**: touch `IN_SCOPE_THEMES` (`:278`) — it governs the **existing** pairings, widening *that* is #61 (vNext) and is what would make #83 blocked; add `editorSelection` pairings (**C5** — a different, unmeasured pairing set, raised as a follow-up, T061); lower the 6:1 lift in `makeTheme`; or change `WCAG_AA_BODY` (4.5), which still governs every other body pairing.
- [x] T056 [US6] **GREEN**: add `assertSyntaxBodyContrast(themes)` to `packages/core/src/config/theme-quality.ts` — it **throws**, naming theme + token + ratio — and call it from the **existing** theme-quality guard suite beside `assertDistinct`/`assertInScopeContrast`. **The same mechanism, not a new one**: a theme that fails **fails the build** (FR-027), never a report nobody reads — routing a failure to `knownContrastIssues()` (`:396-405`) is exactly what #83 objects to. Gate all fifteen bundled themes **bar the by-design carve-out** (C4); they all pass today, measured, so gating them everywhere **cannot fail the build and cannot block on #61**, and the carve-out costs nothing — it exists so a future recolour of Matrix is not reported as a defect.
- [x] T057 [US6] **Prove the derivation, which is the actual requirement** — a hand list would pass every assertion above and still be the bug ([quickstart.md](./quickstart.md) §#83): add `syntaxDecorator: '#0d1017'` to `THRONG_THEME.colours` in `packages/core/src/config/theme.ts`, run `npx vitest run packages/core/tests/unit/theme-syntax-body-contrast.test.ts` and confirm it goes **RED with no test edited**, then `git checkout packages/core/src/config/theme.ts`. If it passes, the list is still hand-written somewhere and **FR-026/SC-013 are unmet**. Leave no artifact behind (Principle V).
- [x] T058 [US6] Run `npx vitest run packages/core/tests/unit/theme-syntax-body-contrast.test.ts` — 4 RED green, 16 green **still green** (the measured 150, locked in). Then the neighbouring theme guards: `packages/core/tests/unit/theme-quality*.test.ts`, plus `packages/ui/tests/e2e/theme-sweep.e2e.ts`, `theme-tokens.e2e.ts`, `default-themes.e2e.ts` — `CONTRAST_PAIRINGS` just grew by ten entries per theme and `SYNTAX_TOKENS` changed provenance.

**Checkpoint**: the property that held by accident now holds by construction, at a **stated** threshold.

---

## Phase 8: Polish & Cross-Cutting

- [x] T059 **Documentation currency (constitution v3.10.0 — an obligation discharged by this task, not a waiver, and it lands in THIS change)**: `README.md` — a settings key **disappears** (`explorer.openMode`), a setting **moves group** (`editor.openOnClick` → File Explorer, "Open files with"), and two controls change shape (a flavour **table**, a built-ins **picker**); ~~`ROADMAP.md` — six v1.0.0 bugs close~~ (**superseded**: master retired `ROADMAP.md` in `5856ea5`, and constitution **4.0.0** now forbids a second forward-looking list in a tracked file — issues and milestones are the single source. The ROADMAP edit was dropped on rebase; the guard's description lives in `README.md` instead); `CONTRIBUTING.md` + `docs/testing.md` — how the `@admin` suite is now run in CI (T031) and that a green CI bar does **not** cover #87 or #86 because those two specs self-skip on an elevated runner — **but it does cover #67**, whose spec has no elevation guard (C18; do not repeat the "six of the seven / all three" claim these docs carried, which was never true); and the **accepted 400ms loss under an uncatchable termination** (FR-011) written down **where a user can find it**, not in a comment nobody reads — the debounce is a deliberate trade against write amplification during a drag, so it is a stated limit, not a defect.
- [X] T060 **Delete `WindowsDeElevator` if and only if no CONSUMER references it** (`packages/platform-windows/src/` — `isAvailable()` hard-returns `false`, found dead during reproduction), along with `packages/platform-windows/tests/contract/windows-de-elevator.contract.test.ts` (**2 tests**) **and its barrel re-export at `packages/platform-windows/src/index.ts:8`**. A YAGNI/Principle VIII cleanup that costs one commit and removes a **decoy** from the next person who debugs this path. **The gate says CONSUMER deliberately**: the barrel *is* a reference, and the only one — read literally, "nothing references it" would block the deletion forever; read as intended, the barrel must go too or `tsc -b` breaks on a re-export of a deleted module. There is no consumer anywhere. **If a real consumer exists, leave it and raise an issue** rather than growing this feature. Also consider `closePreferences()` (`preferences-window.ts:117`) — same dead-code class (its docblock claims "used on app teardown paths"; there are none) — but **only if the sweep stays one commit**; otherwise raise it.
- [x] T061 Raise the follow-up issues this feature deliberately refused to absorb, each with the reasoning already written down: **C5** — `editorSelection` syntax pairings (same class, different pairing set, unmeasured, not named by #83); **C11** — flavour reordering affordances (a capability, not a defect fix); **C13** — renaming a flavour id while re-keying `terminals.defaultParams` (correct but unkind as shipped: "delete and re-add" costs the row's other fields); **Open Question 9** — a fleet-wide "no inert settings" guard, which **MUST specify typed accessors or a type-graph check, never a grep** (a text scan was attempted during reproduction: **11 false positives**, defeated by aliased section objects like `explorerSettings.dragCopyModifier`); **a move onto a path that is itself open** (undefined today, made *expressible* by the move signal — the collision UX); **a per-assumption rather than per-file elevation guard** for E2E, so non-terminal specs stop self-skipping on an elevated runner; and **FR-014 as its own issue with the captured shim reason attached** if T033 found de-elevation genuinely broken. Label every one with a type **and** an `area:*`.
- [X] T062 Run the **full gates**, unfiltered, capturing complete output **once** ([quickstart.md](./quickstart.md) §Definition of done): `npm run lint && npm run typecheck` (zero errors — a lint error is a build failure, v3.13.0), `npm test` — the gate is a **property, not a headcount**: (a) **0 failures**; (b) the **8 named RED guards** from T002's baseline all green (4 `theme-syntax-body-contrast`, 2 `settings-open-on-click-single-owner`, 2 `pty-agent-launch-timeout`); (c) **every delta from T002's baseline is attributable to a task in this feature** — and **no floor figure is stated (C24)**: an earlier draft said "≥ 1508 (T002's 1500 + the 8 flipped)", which its own delta list falsifies (T060 alone deletes 2). The 8 flipped guards are (b); the rest is attribution, not arithmetic — T004, T007, T029, T034, T035 and T036a all **add** node-suite tests (T054 amends an assertion inside an existing test and adds no count); T015, T015a, T015b, T015c, T040, T041a and T048 add E2E, which `npm test` does not count, while T039 and T052 **retire** assertions that encode the defects and **T060 deletes `windows-de-elevator.contract.test.ts` (−2)**. **Do not "fix" a count mismatch by deleting a test**: an exact figure was deliberately removed from this gate because this feature's own tasks falsify it, and an unreachable number gets satisfied by whoever hits it in the cheapest way available. If the count is *below* 1508 or a delta has no owning task, that is the finding — triage it, do not round it. Then `THRONG_E2E_RETRIES=0 npx playwright test` **on a non-elevated machine** (a retried pass is a flake, not a pass). **Put the non-elevated E2E output in the PR evidence**: CI cannot cover **#87 or #86** (their specs self-skip on an elevated runner), and that must be **stated**, never implied. **#67 is not in that list (C18)** — its spec has no elevation guard and executes in CI, so claiming a developer run is needed for it would misstate the coverage in the other direction.
- [ ] T063 Delete every temporary artifact this work created — T057's theme edit, any scratch capture, and the `019-*.log` baselines T002/T003 wrote to the repo root (`rm -f 019-baseline.log 019-e2e-baseline.log`). Being gitignored is **not** the same as being gone: a green bar MUST NOT leave orphaned generated files behind (Principle V). Confirm with `git status --short` — the tree must be clean of anything this feature did not intend to ship.

---

## Dependencies

```text
Phase 1 (T001–T003)   the baseline. Everything below is measured against it.
   │
   │  EXECUTION ORDER (plan §Phase Ordering) — cheapest RED first, one hard dependency:
   │
   ├─► US6 (T054–T058)   pure core, fast, no app run.  ─┐ 6 of the 8 node-suite RED
   ├─► US5 (T048–T053)   pure core, fast, no app run.  ─┘ assertions convert here
   │
   ├─► US3 (T023–T034)   elevation-free integration (~40s) + the CI change (T031),
   │                     which must land before anyone can claim the @admin half is verified
   │
   ├─► US1 (T004–T014)   the P1 data-loss defect; the largest E2E surface
   │       │
   │       └─► US2 (T015–T022)   HARD ORDER: US1 → US2.
   │              US1's re-point writes the new path into the panel config, which rides the
   │              very workspace.save debounce US2 drains. Drain first and US1's persistence
   │              assertion passes for the WRONG REASON; drain after and both are proven.
   │
   └─► US4 (T035–T047, incl. T036a/T041a)   last: the only story that BUILDS a control, and the only one whose
                         blast radius (map-control.tsx) has green callers to protect.
                              └─► Phase 8 (T059–T063)
```

**Within US1**: T005 → T006 (cannot refactor onto a module that does not exist) · T008 → T009 → T011 (the coordinator cannot be wired to a signal that is not emitted) · T009 → T012 (`movedTo` must exist on the stream before the renderer reads it) · T010 must land **with** T009, or the watcher dirties the doc mid-move and AC1's negative assertions fail for the right reason at the wrong time.

**Within US2**: T017 → T019 → T020 (a `void` flush cannot be awaited; a handler that does not exist cannot be acked) · **T015a/T015b/T015c → T017 → T017a → T019** (**RED before GREEN**; T017a builds `settleConfigWrites()` — the one call T019 makes (C22). Without it T019 has nothing to call, and every flush returns `void`, so `await voidFn()` awaits `undefined`, so without it the handler acks having drained nothing and T015a passes for the wrong reason) · T018 → T019 · T016 → T020. **T015, T015a, T015b and T015c all edit `terminate-all-drain.e2e.ts`, so they are NOT parallel with each other** — same rule as US4 ✗ US5 below.

**Within US3**: T023 → T024 → T025/T026 · T028 → T030 → T025 (the connect-lapse message appends the captured launch reason, so the reporter must exist first) · T029 → T030 (**RED before GREEN**) · T027 → T026 (a readiness budget waiting on an ack no agent sends fails **every** terminal) · T031 → T033 (the CI step is what makes the `@admin` half verifiable at all) · **T023 → T031a → T033** (the budgets must exist before they can be forwarded, and they must be forwarded before T033's `$env:` prefix means anything across the UAC hop).

**Within US4**: T035 → T036 → T042 · **T036a → T037** (**RED before GREEN**) → T038 → T039 (the descriptor delta is what turns `settings-metadata.test.ts:86` red; fix them together or the build is red between commits; and `clearable: true` on `terminals.flavours` is only honest **after** T037's `emptyValueFor` arm — declare it before and the clear writes `''` into an array) · T040 → T041 → T041a → T042 (**the text cell is a precondition for the flavour table, and C14's regression test comes first**; T041a's C17 duplicate-from-file case is RED before T042 implements the mode) · T044 → T045.

**Within US5**: T048 (**RED first**) → T049/T050/T051 → T052 (deleting the key is what turns the four bug-encoding assertions red; they move in the same commit) → T053.

**Within US6**: T054 (**the amendment, a test, first**) → T055 → T056 → T057/T058.

## Parallel opportunities

- **US6 ‖ US5** — different files (`theme-quality.ts` vs `app-settings.ts`/`open-intent.ts`), both pure core, both fast. The two fastest paths to a green bar under the rest.
- **US3 ‖ US1** — daemon + platform-windows vs ui/main + core/fs. No file overlap.
- **T004 ‖ T007** — two RED tests, two packages (`packages/core/tests/unit/path-id.test.ts`, `packages/ui/tests/integration/files-service.test.ts`).
- **T035 ‖ T040** — a core unit RED and a ui E2E RED, different files.
- **T015 ‖ T048 ‖ T029** — three RED tests in three different files, three packages. **T015a, T015b and T015c are not in
  that set**: they edit the same file as T015 (`terminate-all-drain.e2e.ts`), so they run **after** it.
- **T035 ‖ T036a ‖ T040 ‖ T041a** — four RED tests, four different files (`flavour-record.test.ts`,
  `metadata.test.ts`, `preferences-map-control.e2e.ts`, `preferences-terminal-flavours.e2e.ts`). T041a
  is `[P]` on that basis — no file conflict — even though it sits inside the `T040 → T041 → T041a →
  T042` chain: `[P]` marks *what may be written concurrently*, not what may be **run** out of order.
- **US4 ✗ US5** — **NOT parallel**: both edit `packages/core/src/config/settings-metadata.ts`. Sequence them.
- **US1 ✗ US2** — **NOT parallel**: a hard order (above), not merely a file conflict.
- Anything touching `packages/ui/src/main/main.ts` (T011, T020, T044) serialises on that file.

## MVP scope

**Phase 1 + Phase 2 (US1)** — the only defect in this sweep that **destroys user work by acting on a lie**: the dirty dot invites the save, and the save undoes the move. It ships alone, needs nothing from the other five stories, and is the whole of SC-001/SC-002.

The **cheapest** coherent increment is **US6 + US5** — pure core, no app run, and 6 of the 8 node-suite RED assertions converted — which is why the plan builds them first even though they are P3 by value. They deliver nothing a user notices; US1 is the smallest increment a user would.

---

## Phase 9: Convergence

**Assessed 2026-07-17 against the tree, not against a diff.** All six stories are implemented; lint and typecheck are clean; the unit, integration and contract suites are **fully green** (the 8 RED guards T002 baselined have all converted — `npm test`'s fail-fast chain reaches E2E, which it could not do with a node-suite failure outstanding). FR-006, FR-007, FR-011, FR-018 and FR-026 were each checked at the level the requirement states rather than the level its tests reach, and each holds. **T060's gate is verified MET and its work is still owed** — `WindowsDeElevator` has **no consumer** (only `packages/platform-windows/src/index.ts:8`'s barrel re-export and its own contract test; `node-pty-host.ts` takes `IDeElevator` but defaults to `passthroughDeElevator`, and `IDeElevator`/`runDeElevatorContract` are live and must **stay**). T060 already carries that work and is not duplicated here.

The two items below are the only unbuilt work with **no task to complete it**: T061 is ticked, so nothing else will reach them. Its own text names **seven** follow-ups; **four** were raised (#105 C5, #106 C11, #107 C13, #108 OQ9), the seventh (FR-014) is not owed until T033 runs, and these two were never raised at all.

- [X] T064 Raise the follow-up issue for **a move onto a path that is itself open** — undefined today, and made *expressible* by US1's move signal (two documents would claim one path in the one-buffer registry, where today the collision merely goes unnoticed). Record that registry **integrity** is already handled — `markMoved` re-keys via `unregisterPanel` + `registerOpen`, the pair `save()` uses for Save-As — so what is open is the collision **UX**, not corruption. Label it with a type **and** an `area:*`, per T061 · plan §Complexity Tracking (*"recommend an issue for the collision UX"*) · spec §Edge Cases · research §1 (missing)
- [X] T065 Raise the follow-up issue for **a per-assumption rather than per-file E2E elevation guard**, so non-terminal specs stop self-skipping on CI's elevated runner. `skipIfElevated()` is applied file-wide to `editor-move-repoint.e2e.ts` and `terminate-all-drain.e2e.ts`, neither of which starts a terminal — so `admin.ts:40-52`'s reasoning (an elevated daemon routes terminals through the de-elevated agent) applies to neither, yet both are excluded from CI, which is why #87 and #86 rest on a developer's non-elevated run (T062). Label it with a type **and** an `area:*`, per T061 · plan §Complexity Tracking (*"Recommend a follow-up issue to make the elevation guard per-assumption rather than per-file"*) · C18 (missing)
