/**
 * Root-confinement helpers (005 Phase C / US4): keep or return a terminal's
 * working context to the project root (best-effort, flavour-dependent).
 *
 * STUB: implemented test-first in Phase C (T070/T071).
 */
export {};
