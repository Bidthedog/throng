{
  "note": "The declared ceiling on the E2E suite (034 FR-060, constitution v5.0.0 Principle V). A RATCHET: these numbers may only go DOWN. The build fails when a count exceeds its budget, AND when a count falls below it - because a ratchet that is never tightened is not a ratchet, it is a ceiling nobody is holding. Lower the number in the same commit that removes the tests. Raising one is a deliberate act that a reviewer will see in the diff, which is the whole point: the rule this replaces failed because growth was invisible. Before raising it, answer in one sentence what the new test can assert that a unit, component or integration test cannot.",
  "measuredFrom": "spec 034, after tagging; the migration batches lower these as they land. 041 raised @window 191→192 for one E2E: that a real shell does not swallow Ctrl+Alt+M, which no unit, component or integration test can observe because only a real ConPTY forwards keys to a shell. The total reads 560 rather than 559 because #290 raised @terminal 102→103 on master while 041 was in flight, and the two additions are independent - a rebase conflict that BOTH sides had written as 559, which is the arithmetic this ratchet exists to catch. 560->561 and @terminal 103->104 for the second half of #290, the mouse wheel: it asserts that a wheel notch still reaches a full-screen program after its panel is rebuilt, which needs a real daemon DECIDING what to replay (it withholds the tail entirely for an alt-screen program), a view genuinely torn down and rebuilt from that decision, and a real ConPTY carrying the mode sequences - no test in this repo constructs a real xterm Terminal at all, so the DEC-mode snoop and the replay do not exist on any cheaper layer. 561->562 and @terminal 104->105 for #326: that a wrapped OSC 8 hyperlink underlines on every row it occupies, with a wrapped PLAIN url as the control in the same panel - the underline is an inline style the DOM renderer writes per cell on hover, so it needs a real xterm, a real wrap and a real pointer, and the two link paths must be compared under identical width to tell which provider is at fault. 562->563, @terminal 105->106 and @window 192->193 for #199: that a window opened by a command running in a terminal ends up in the FOREGROUND rather than behind throng. The assertion is which top-level window Windows puts in front, which no lower layer has - there is no foreground to lose without a real window, a real desktop and a real process chain (throng -> daemon -> conhost -> shell -> the command -> its window), and the thing under test is precisely whether the OS honours a handoff across that chain. The policy either side of the OS call - when throng asks, and what a platform without the concept does - is pinned in packages/core/tests/unit/foreground-handoff.test.ts and deliberately NOT duplicated here. 563->565 and @window 193->195 for #382: that step-left is enabled once a reveal has moved the strip by less than one tab, and that a right-click on either step control moves nothing. The step controls render only when the strip OVERFLOWS, which is measured from laid-out widths - jsdom reports every rect as 0x0, so they never render in component/tab-strip.test.ts (its header records the decline). The arithmetic behind both is pinned in packages/core/tests/unit/tab-strip.test.ts; what is left here is the control's enabled state and which mouse button it answers. 565->568 for spec 043's User Story 5, which is the whole of what that feature spends at this layer - three tests against 51 declarations at the four cheaper layers, and the E2E line item was written into tasks.md as 'only sub-workspace syncing, real restart, and the two window chords' before any of them existed. @explorer 39->40, find-in-files-subworkspace.e2e.ts: that a Find in Files panel synced into a sub-workspace mirrors its parent's results, survives its own view being closed, and is closed when the parent panel is destroyed. Every clause is about TWO Electron windows and the relationship between them - a second BrowserWindow created by a sync, one panel id living in both, a destroy in the first that must reach the second and a close of the second that must not reach the first - and there is no window to open below this layer; subworkspace-sync.test.ts can assert what the store does when a message arrives and cannot make a window arrive. @persistence 0->1 (a NEW category entry: the tag existed in the closed vocabulary and no test carried it), find-in-files-restart.e2e.ts: that the panel a running application WROTE is the panel a second running application RESTORES, with its term, match modes, scope, replace toggle and replacement, and with no results and no pending preview. find-in-files-persistence.integration.test.ts already pins the blob round-trip and writes the row itself; what it cannot reach is the debounced save, the daemon that owns the store, the panel-type dispatch that decides what to mount and the startup restore order - four mechanisms in processes that have to be started and stopped for the question to mean anything. @window 195->196 and @core 38->39, window-chord-resolution.e2e.ts: that Ctrl+Shift+F and Ctrl+Shift+H reach the resolver through the keepShift LETTER branch. It is @core because that branch's failure mode is silence - the event is built with one modifier too many, resolves to null, and the chord is simply inert, with no throw and no log - and window-chord-manifest.test.ts's COVERED map already names both actions as pressed HERE, so an uncovered claim would be a command that can die without a test going red. The manifest guard asserts the allowlist and the branch predicate at the unit layer without pressing anything, which is precisely why the pressing half is irreducible. 568->569 and @window 196->197 for 043 T132 (FR-078, #380, US5 scenario 6), find-in-files-subworkspace.e2e.ts: that a Find in Files panel synced into a sub-workspace SHOWS the parent's results and follows them as they change. What no cheaper layer can assert is that a SECOND renderer process - its own store, its own subscription to the scan channel - ends up displaying rows it never asked for: find-in-files-snapshot.test.ts pins what one panel does with a snapshot when one arrives and file-search-viewers.integration.test.ts pins that main sends one to every viewer, but neither can make a second window exist and then ask what it is showing, which is the exact span where the defect lived. 043's figures were first written against 563, before #382 landed on master; they are restated above against master's 565. Both sides had written @window as 195 - master for #382's two tests, 043 for its own two - so a rebase conflict resolved by taking either side would have kept 195 and silently dropped two declarations. The correct merge is 197, the same arithmetic this note already records for 559. SPEC 044 spends five E2E declarations (R22) and does not raise the budget for them: five unreserved declarations whose assertions a lower layer already holds move down FIRST, each observed failing at that layer against a deliberately broken implementation before the E2E was deleted. 569->568 and @editor 117->116 (044 T163a): editor-feedback.e2e.ts 'Open In offers New Editor (a second panel) and disables it once the file is open' - an in-document React menu row's enabled class plus a click that opens a second panel, held by unit/open-in-targets.test.ts and component/explorer-open-in-target.test.ts (the alreadyOpen rule, offered-but-disabled) and component/editor-open-router.test.ts with component/editor-open-routing.test.ts (the 'new' target reaches openFileInTab, which adds a panel). 568->567 and @editor 116->115 (044 T163b): editor-language-override.e2e.ts 'a persisted language this build no longer knows opens as plain text, WITHOUT error, and is preserved (FR-005b)' - a precedence decision and a persisted value, held by core/tests/unit/language-precedence.test.ts with ui/tests/integration/language-detect.integration.test.ts (the fall-through), ui/tests/unit/language-loaders.test.ts (no throw), daemon/tests/integration/document-ipc.integration.test.ts (the store keeps the id) and ui/tests/unit/language-override-load.test.ts, written for the move (opening the file writes nothing back). 567->566 and @editor 115->114 (044 T163c): editor-feedback3.e2e.ts 'the editor pill shows the containing folder in brackets (subfolder + root)' - a formatted string, two spans and a title attribute, held by core/tests/unit/path-display.test.ts (the folder part per path shape) and component/panel-box.test.ts, extended for the move (the header draws folder and name into their own spans and titles the pill with the back-slash-only full path). 566->565 and @editor 114->113 (044 T163d): editor-feedback2.e2e.ts 'the Files & Folders context menu has a New Folder action' - an in-document React menu row and a create addressed to a folder, held by unit/menu-sections.test.ts (the row), integration/files-service.test.ts (newFolder makes a real 'New folder') and component/explorer-root-menu.test.ts, extended for the move with the SUBFOLDER case the E2E drove (the create is addressed to the right-clicked folder and the rename box opens on the new node inside it). 565->564 and @window 197->196 (044 T163e): tab-settings.e2e.ts 'T057 - a value outside a declared range is refused, and the last valid one stands' - a number control's validation and a value therefore never written, held by component/preferences-number-control.test.ts (above-maximum and, added for the move, below-minimum refused with the invalid marker shown and nothing committed) and core/tests/unit/tabs-settings.test.ts (the declared ranges, and the guard clamping a value outside them). That is the floor: 564 total, @editor 113, @window 196, before the five 044 declarations are added. 564->565 and @editor 113->114 (044 T163), preview-hostile.e2e.ts, @reserve:runtime: that a real Chromium, under the real CSP and the real main-process request filter, runs nothing from the hostile fixture and requests nothing but its own renderer assets and throng-preview:, and requests no https image from remote-images.md once Load remote images is turned off on disk. jsdom neither enforces a CSP nor fetches an image, so the substitute cannot hold the property; observed red against a renderer that ignored the setting and a sanitiser that kept onerror. 565->567 and @editor 114->116 (044 T164), preview-scroll.e2e.ts, two @reserve:layout declarations: what a real layout keeps in view - the heading a reader was looking at through a live update of a parented preview (FR-024), and the place a reader left a document through Back twice and Forward once (FR-107). scroll-anchor.test.ts pins the arithmetic with rects supplied by hand; jsdom reports every rect as 0x0, so where the reader actually ends up exists only in a running Chromium. Observed red against an update that did not restore its anchor, a history step that ignored the stored place, and a covered body drawn with display:none. 567->568 and @editor 116->117 (044 T165), navigation-history-keys.e2e.ts, @reserve:input: that a real Alt+Left in a focused CodeMirror goes Back and the editor's own cursorSyntaxLeft on those keys never runs (FR-105). window-arrow-chords.test.ts presses a synthesised event through the window listener, which asserts the event the test built rather than the engine's dispatch order to a focused contenteditable; observed red (the caret moved from Col 31) against a listener that ran Back without stopping the event. 568->569 and @window 196->197 (044 T166), preview-subworkspace.e2e.ts, @reserve:window: that a parented preview synced into a sub-workspace window follows its parent being typed into in the main window (FR-022), and that Back pressed in the sub-workspace window moves the main window's preview (FR-110). Two renderers are the assertion - the integration tier pins that main sends every viewer the same revision, and cannot make a second window exist to ask what it shows. Observed red against main pushing updates to the first viewer only, and against a history step answered to the requesting window alone. @core stays 39, and the budget now reads exactly what it read before 044's Phase 10: 569, @editor 117, @window 197. 569->570 and @editor 117->118 (044 T183, iteration 2026-09-15), preview-scroll.e2e.ts, @reserve:input: that a real mouse drag across a preview's body selects its text, and that a Ctrl+drag on a link selects rather than follows (FR-035, FR-094). Whether a drag starts a selection is the engine's input handling against the cascaded user-select; jsdom applies no stylesheet and the Range API every component copy test selects with ignores user-select, so those tests passed while no user could select anything. unit/preview-text-selection-css.test.ts pins the cause. This one was RAISED rather than paid for, because the planned demotion (T181) was skipped by ruling - and that ruling was reversed. 570->569 and @editor 118->117 (044 T218, performing T181): editor-indicators.e2e.ts 'auto-save writes edits within the debounce without Ctrl+S' - an Electron app with a seeded THRONG_CONFIG_ROOT, typing into a real CodeMirror and polling a real file, for a claim that splits across two cheaper layers. The firing half is component/editor-update-listener.test.ts's new 'auto-save writes the edit without Ctrl+S (006 FR-060)' block, which mounts the real EditorView behind the real editor.* bridge: that file already owned the ARMING (a timer scheduled at the debounce) and had no assertion that the armed timer did anything, so a callback that saved nothing was green at every layer. Two of its four cases are new coverage outright - the debounce itself (two edits, one save) and FR-060's unpathed clause (no write AND no unasked chooser). The disk half is integration/editor-service-save.integration.test.ts, which writes real files through the real EditorService and asserts encoding, BOM and line-ending preservation - strictly more than readFileSync(...).toContain('AUTO'). Observed red twice, against two deliberate breaks to use-editor.ts: the timer body emptied reds the two firing cases, the arming guard removed reds the two controls, and no case is red under both. reserve-tag-debt.json 116->115: the declaration carried no @reserve:* tag. The budget now reads what it read before 044's Phase 10 and again before T183: 569, @editor 117, @window 197. 569->568 and @editor 117->116 (044 T226, iteration 2026-09-16): quick-open.e2e.ts 'a file inside an excluded folder is never listed (AS-12, FR-006)' - a Quick Open query whose only matches sit under .git and node_modules, expected to list nothing at the shipped settings. Quick Open lists what ProjectFileIndexService pushes and main hands that service explorer.excludeGlobs, so the claim is the shipped list applied by a real walk: integration/project-file-index.integration.test.ts 'AS-12 - at the SHIPPED exclude globs, nothing under .git or node_modules is listed', written for the move over a real tree and a real watcher with the deep tree's two files at their paths, observed red with the node_modules glob dropped from DEFAULT_EXCLUDE_GLOBS; core/tests/unit/app-settings.explorer.test.ts pins that the shipped setting is that list; and quick-open-target.e2e.ts (SC-018) still types the same query into a real modal at the same default. reserve-tag-debt.json 115->114: the declaration carried no @reserve:* tag. 568->569 and @editor 116->117 (044 T237), preview-scroll.e2e.ts, @reserve:layout: two-way scroll sync in a real layout - a mouse wheel over a parented preview moves the real editor's top line to the heading brought to the top, Go to Line and Ctrl+End in the real editor move the preview's top block to the editor's, both scrollers hold still across five animation frames after each, and Synchronise Scrolling switched off from the editor's status bar stops both directions until the preview's turns it back on. jsdom lays nothing out and fires no scroll event for a scrollTop write, so whether the echo marks agree with Chromium's and CodeMirror's real event order exists on no cheaper layer. The budget reads what it read before the iteration: 569, @editor 117, @window 197. 569->570 and @terminal 106->107 for #198: a Ctrl+click on a link in a program that owns the mouse must not ALSO be forwarded to that program, which needs a real xterm, a real ConPTY carrying the DEC mouse modes and a real program reading its input - no test below E2E constructs an xterm. 570->571 and @terminal 107->108 for 045 round five (#408, FR-060): that with editor.links.detectInTerminals OFF a program-DECLARED OSC 8 hyperlink is as inert as a plain url - no mark, no underline, no hand, no status readout, no native title on the panel, and a Ctrl+click that opens nothing. The three seams the widening added (oscLinksInView, setHoveredUri, openTerminalLink) live inside use-terminal.ts's mount closure and are not exported, no test in this repo constructs a real xterm Terminal, and a declared hyperlink does not exist until a real ConPTY has carried the OSC 8 sequence around its text - so the OFF state of the switch over a program-emitted link has no cheaper layer; link-detection-switches.test.ts already holds everything the provider and the view pass decide. 571->572 and @editor 117->118 for #384, editor-gutter.e2e.ts, @reserve:input: that a real double-click on a gutter line number, and a real drag down the gutter, select no gutter text while a double-click in the document body still selects a word. Whether a mouse gesture starts a selection is the engine's input handling against the cascaded user-select - jsdom applies no stylesheet and selects nothing by pointer - and the defect was exactly that cascade: theme.css re-enabled selection on .cm-editor and it reached .cm-gutters. Observed red (a double-click on line 2 selected \"2\") before the fix.",
  "wrappedDeclarations": "The category counts come from a PER-LINE regex that wants the name and the { tag: [...] } on the SAME line as test(. A declaration wrapped across several lines - which is what a formatter does to a long one - is still counted in the total, but its tags are invisible, so it belongs to no category at all. Measured 2026-08-27: @window read 191 against a budget of 192 while the total was correct, which is a confusing failure to arrive at from the other direction. Keep the declaration on one line.",
  "countingBasis": "DECLARATIONS, not executed tests. Playwright reports a larger number for the same suite, and neither is wrong: four spec files declare tests inside a module-level loop over shell flavours, so one declaration becomes several executed tests. Declarations are what a budget should count - it is a limit on what people WRITE, and a table-driven test that gains a fifth flavour has not made the suite harder to maintain. This total also includes the @admin and @quarantine tests that grepInvert removes from a default run.",
  "total": 572,
  "core": 39,
  "byCategory": {
    "@editor": 118,
    "@explorer": 40,
    "@failure": 21,
    "@persistence": 1,
    "@prefs": 88,
    "@terminal": 108,
    "@window": 197
  }
}
